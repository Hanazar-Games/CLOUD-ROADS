# 开发记录

## Phase 0 — 完成

完成 Vite + TypeScript 项目、Three.js 场景与透视相机、基础光照、天空色、游戏循环、窗口缩放、输入管理、开发者飞行相机与 F3 调试界面。

文件范围：项目配置、`index.html`、`src/main.ts`、`src/style.css`、`src/game/`、`src/camera/`、`src/input/`、`src/debug/` 和基础测试。

验证：build、lint、游戏循环测试和实际浏览器渲染／操作测试均通过。Phase 0 的临时平面在 Phase 1 被流式地形替换。

## Phase 1 — 完成

文件范围：`src/world/`、`src/terrain/`、游戏集成、地形／生命周期／长距离模拟测试及浏览器测试。

职责分配：

| 模块 | 职责 |
| --- | --- |
| WorldSeed / Noise / HeightFunction | 不依赖渲染器的确定性世界采样 |
| TerrainGenerator / TerrainTopology | 属性生成与 LOD 公共边界连接 |
| TerrainWorker / TerrainWorkers | 有限并行任务、数据转移、超时及错误反馈 |
| ChunkPlanner / ChunkManager | 空间覆盖、优先级、过期结果丢弃、LOD 替换与网格池 |
| TerrainChunk | 复用几何、更新 GPU 属性和包围球 |
| World / FloatingOrigin | 种子世界生命周期、逻辑坐标和渲染坐标分离 |

测试覆盖：

- 同种子同坐标高度一致，异种子产生不同地形。
- 正负世界坐标、相邻边界的高度／法线／颜色一致。
- 64 / 16 / 8 格的跨 LOD 边界一致；三角形朝向与总覆盖面积正确。
- 10 / 30 / 100 公里模拟中的有限数值、有限活动范围和原点重定位。
- 远距离瞬移后丢弃旧任务、卸载后复用几何、世界销毁后忽略迟到结果。
- Worker 失败时显示错误并停止继续调度，可重新加载种子恢复。
- 生产构建中实际飞行超过 30 公里，至少五次原点重定位；停下后恢复 289 分块。
- 种子往返切换后恢复相同起点，旧世界的网格和 Worker 被释放。

Phase 1 验收结果：20 项单元测试、3 项 Playwright 端到端测试通过，TypeScript build 和 ESLint 通过。

性能抽测：本机 Apple M5 Pro、Chromium / ANGLE Metal、1920×1080，默认种子静止观察约 60 FPS、99 次 draw call；以 120 m/s 连续飞行 10 秒，平均 60 FPS，最大帧间隔约 16.8 ms，没有记录到超过 50 ms 的主线程任务。这是单机短时抽测，不代表所有硬件。`npm run benchmark` 可复测连续飞行。SwiftShader 软件渲染明显较慢。

## Phase 2 — 完成

新增 `RoadGenerator`、`RoadSegment`、`RoadSpine`、`RoadDebug`，接入 World、自由相机、道路观察按钮和 F3 道路指标。

候选路线同时评价地形落差、坡度、转弯、悬崖和种子生成的方向引导。相邻控制点使用显式端点切向量的三次 Hermite 样条；位置和切线连续，避免未经约束的插值超调。弧长表用于道路里程采样，生成结果不依赖帧率或每帧批量大小。

当前采用向北单调前进的走廊，直接排除自交。普通坡度目标 ±6%，每段端点坡度变化不超过 2%；转向每段最多 18°。10 / 30 / 100 公里自动测试逐段检查曲线内部采样点的坡度 ≤10%、半径 ≥100 m、连续性及有限数值。

生成窗口预留前方约 2300 m、后方约 1600 m（南北投影），最多驻留 96 段。远距离返回已释放区域时，从种子起点以每帧预算分批重建，不永久保留完整路线。

Phase 2 验收：build、lint、26 项单元测试和 4 项 Playwright 测试通过；已在实际场景检查中心线与控制点。

## Phase 3 — 完成

新增 `RoadMesh`、`RoadMaterial` 与网格测试。路面为 8 m 沥青，两侧各 1.2 m 路肩；原创 GLSL 绘制中心虚线和边线，保留 Three.js 的光照、雾和色彩转换。横坡抬高弯道外侧，控制点之间连续过渡。

道路网格与骨架共用样条采样缓存，每段 24 个间隔；只在窗口变化时更新预分配的顶点、法线和 UV 缓冲。路面只增加一次 draw call；浮动原点仅更新对象平移。UV 使用附近的 12 m 标线周期原点，避免长期累计里程降低标线精度。

验收覆盖网格宽度、三角形朝向、横坡方向、UV 连续性、缓冲复用、重定位与释放。生产浏览器新增向北飞行超过 30 公里的道路流式生成测试，并验证返回后重建、种子切换与 shader 无错误。

Phase 3 最终验收：build、lint、29 项单元测试、5 项 Playwright 测试全部通过。相同 M5 Pro / Metal / 1080p 环境下向前飞行 10 秒，道路窗口从 25 段扩展至 38 段，约 60 FPS、最大帧间隔约 16.8 ms，没有记录到超过 50 ms 的主线程任务。该短时结果不代表所有硬件或长期运行表现。

## Phase 4 — 完成

新增 `RoadFrame` 与 `RoadCorridor`，调整 `TerrainGenerator`、Worker 数据协议、`TerrainTopology`、`TerrainChunk`、`ChunkManager` 和 `World`。路面与地形共用道路横坡坐标系；道路中心两侧 12 m 路基位于路面下方约 0.08 m，向外按自然地形高差平滑切坡或填方，最大影响半径 160 m。

道路窗口扩大为前后各约 2600 m（南北投影），最多保留 96 段。覆盖就绪后，按道路版本缓存走廊，并给每个 Worker 任务发送局部快照，避免先生成未耦合地形。当前走廊查询依赖路线向北单调前进；下一阶段加入发卡弯时需要同步调整空间查询。

所有 LOD 的公共边界改为 4 m 间隔，粗网格内部通过环带三角形连接细边界；沿道路分块提升为 64 格，保证路基精度。测试检查跨分块／LOD 的高度、法线、颜色，以及三角形面积、朝向和边的连接次数。

新增切坡、填方、横坡、局部快照复现与生成顺序测试。两个种子的实际路面和地形网格使用 Three.js Raycaster 检查中心及两侧路肩，测试采样处间距均在 0.01–0.2 m。浏览器验证沿路 LOD 提升、30 公里飞行、重定位、返回起点和种子切换；已目视检查连续路基。

Phase 4 验收：TypeScript / Vite build、ESLint、34 项单元测试与 5 项 Playwright 测试全部通过。Apple M5 Pro / ANGLE Metal / 1920×1080 向前飞行 10 秒约 60 FPS，p95 帧间隔 16.7 ms、最大 16.8 ms，没有记录到超过 50 ms 的主线程任务。结束时活动分块 289 个、LOD 分布 36 / 50 / 203、94 次 draw call、约 19.6 万三角形。这是单机短时抽测，不代表所有硬件或长期运行表现。

## Phase 5 — 完成

调整 `RoadGenerator`、`RoadSegment`、`RoadSpine`、`RoadCorridor` 与 World 集成，新增 `RoadIndex`。普通候选路线仍使用地形评分；当地形高差超过阈值时，进入确定性的山路序列：渐变转向、横向爬升／下降、150° 折返弯、横向连接段、反向折返弯、退出。弯心最小半径约 36.7 m，弯间连接段至少 384 m，每组结束后至少间隔 4 km 才能触发下一组。坡度限制为 ±6%，规划状态随控制点保留，增量生成和重放结果一致。

道路改为按水平弧长积分平滑方向与坡度，端点曲率及竖向坡度变化率归零，消除旧三次 Hermite 路段的曲率跳变。横坡随实际曲率变化，上限 6°。每段使用 48 个采样间隔，普通路段长 96 m、发卡弯长 144 m；最多驻留 160 段，为折返增加的里程留出有界容量。

二维包围盒树用于最近路段查询和走廊裁剪，替换依赖 Z 单调性的二分查询；Worker 接收独立线段，裁剪后不会错误连接分离的路段。当前生成器仍保持北向推进，索引已覆盖反向道路。道路视角和里程改用水平距离最近的路段，新增「发卡弯视角」及 F3 发卡弯数量；俯瞰相机按耦合地形抬高并指向弯心。

修复回退时地形覆盖不足的问题：即使相机还在已保留的路线内，也会检查后方 2600 m 走廊并按需重放，避免新加载地形缺少道路数据。

测试按 TDD 加入曲率／横坡连续、上坡与下坡折返、弯间净距、反向走廊、空间索引与穷举结果一致、回退覆盖和俯瞰相机避开山体。真实路面／地形射线检查扩展到整个驻留窗口，包含急弯与两侧路肩。10 / 30 / 100 公里道路模拟、种子复现、有界内存和浏览器长距离飞行继续通过。

Phase 5 最终验收：TypeScript / Vite build、ESLint、43 项单元测试和 6 项 Playwright 测试全部通过；已在浏览器目视检查连续的两级折返路线及观察相机位置。

性能抽测：Apple M5 Pro / ANGLE Metal / 1920×1080 连续飞行 10 秒约 60 FPS，p95 帧间隔 16.7 ms、最大 16.8 ms，无超过 50 ms 的主线程任务；结束时活动分块 289 个、LOD 分布 40 / 49 / 200、96 次 draw call、约 21.6 万三角形。这是单机短时抽测，软件渲染浏览器的帧率不具代表性。

## Phase 6 — 完成

新增 `src/bridge/BridgeDetector.ts`、`BridgeMesh.ts`，接入 `World`、`RoadCorridor`、流式道路窗口和观察界面。先完成检测、谷地保留、几何支撑和生命周期测试，再实现桥梁生成。

检测使用自然地形：道路高出谷底 40 m 的连续距离至少 80 m、两端路面与自然地面的高差不大于 12 m、总桥段长度不超过 1400 m；桥跨内部还检查两侧净空，避免桥面穿入山体。缺少另一端落点、过长、浅谷和短沟不生成桥梁。高度采样只缓存当前道路窗口，随卸载删除。

道路前后预留范围扩大为 4000 m（南北投影），最大驻留 256 段，包含地形窗口、走廊影响范围与完整桥梁的检测距离。桥梁判定完成后才把带地基权重的局部线段快照送入 Worker。桥跨内部保留自然谷地，桥头 32 m 范围平滑接回原有切填方；100 公里模拟验证移动窗口内的判定和地形一致性。

路面继续复用原有沥青网格，桥面下方增加 2 m 厚混凝土底梁与两侧护栏。桥墩约每 32 m 设置一次，两端增加桥台，基础同时参考自然地形与耦合地形并埋入地下。桥面／护栏和支撑结构各使用一个固定容量的 `InstancedMesh`，共增加最多两次 draw call；坐标重定位只平移两个网格，流式更新复用实例缓冲，换种子和销毁时释放共享资源。

新增「桥梁视角」及 F3 桥梁／支撑数量。道路仍在生成时保留上一批桥梁，避免桥梁闪断；加载提示区分路线生成与地形加载，观察按钮等路线就绪后启用。

测试包括合成深谷的正反例、桥侧侵入拒绝、100 公里判定稳定与缓存上限、桥头过渡、局部 Worker 快照复现、实际曲线桥面的射线间距、桥墩基础、缓冲复用、浮动原点与资源释放。两个种子的射线检查验证混凝土顶面位于沥青下方、桥下地形与自然高度相符；浏览器覆盖桥梁观察、30 公里往返和换种子后恢复同样桥梁。

Phase 6 最终验收：TypeScript / Vite build、ESLint、52 项单元测试及 7 项 Playwright 测试全部通过；已在浏览器检查桥面、护栏、桥墩与保留的谷地。

性能脚本先等待路线就绪及地形加载完成，再测量飞行。Apple M5 Pro / ANGLE Metal / 1920×1080 连续飞行 10 秒约 60 FPS，p95 帧间隔 16.7 ms、最大 16.8 ms，无超过 50 ms 的主线程任务。末帧活动分块 289 个、97 次 draw call、约 25.2 万三角形，驻留 3 段桥梁、74 个支撑（含桥台）。这是单机短时抽测，不代表所有硬件或长期运行表现。

## Phase 7 — 完成

新增 `src/biome/BiomeSystem.ts`，使用独立种子噪声与逻辑世界坐标采样温度、湿度、雪线和地表颜色。海拔降低温度，局部气候改变森林过渡和雪线高度；山谷、森林、岩石、高山与雪区使用归一化的连续权重混色。雪线以 3000 m 为基准，叠加大范围气候与局部噪声，跨越 360 m 高度区间平滑积雪；陡坡减少积雪并露出岩石，避免水平白色条带和整面雪墙。

`TerrainGenerator` 删除原有简易颜色公式，在 Worker 内按道路耦合后的高度和法线采样地表。每个分块复用一个采样对象，继续写入原有颜色缓冲，不增加材质、网格或 draw call。世界地面查询与 Worker 使用相同算法，桥梁下方采样自然谷底，桥头与路肩采样耦合后的地面。

F3 新增地面生物群系、海拔、坡度、积雪覆盖、当地雪线、温度与湿度，每 150 ms 采样一次。只升降自由相机不会改变地面指标，底部海拔仍表示相机高度。界面阶段更新为 `/ 07`。

按 TDD 验证种子及采样顺序独立、海拔类型过渡、空间雪线、陡坡裸岩、权重归一化、颜色连续与复用对象。扩展跨 LOD 颜色接缝、耦合地形配色和 10 / 30 / 100 公里有限颜色检查；世界测试验证桥下地面与原点重定位后的采样一致，浏览器测试验证升降飞行和换种子复现。

Phase 7 验收：TypeScript / Vite build、ESLint、58 项单元测试及 8 项 Playwright 测试全部通过。Apple M5 Pro / ANGLE Metal / 1920×1080 连续飞行 10 秒约 60 FPS，p95 帧间隔 16.7 ms、最大 16.8 ms，无超过 50 ms 的主线程任务；末帧 289 个活动分块、97 次 draw call、251692 个三角形。这是单机短时抽测。

## v0.1.1 — 检查与修复

完成跨层检查并修复地面指标与坐标不同步、输入焦点残留、飞行快捷键默认行为、图形恢复按键残留、鼠标锁定失败无提示，以及 Worker 故障释放问题。F3 地面指标改为每 250 ms 与坐标一起采样。三种观察方法在路线未就绪时拒绝跳转。

增加当前世界重试、暂停和面板收起入口，矮窗口控制面板可滚动，路线骨架默认关闭。版本升级为 0.1.1，新增页面公告与历史入口；0.1.0 原本没有独立公告，按开发记录补录到历史区。图形启动失败时公告仍可用。SFX / BGM 尚未实现，界面及公告已明确说明。

详细问题、验证和未覆盖范围见 [v0.1.1 检查报告](audit-v0.1.1.md)。本次没有进入 Phase 8。

## Phase 8 — 完成（v0.1.2）

新增 `CloudField`、`CloudMaterial` 和 `CloudSystem`，接入 Game 生命周期、观察入口与 F3。云场由种子生成一张 128×128 RGBA 纹理（64 KiB），包含周期噪声、云顶团块和细节；云底 1800 m，云顶在约 2300 m 附近起伏。逻辑坐标与风的偏移按 8192 m 周期取模，CPU 雾采样和 GPU 使用同一纹理，跨正负坐标与浮动原点保持一致。

场景先渲染到带深度纹理的复用目标，再用一个全屏合成处理八层云。云的透明度随云场、高度、视线与山体深度变化；与地形、道路、桥梁相交处柔和淡出，远处融入雾色。支持浮点颜色缓冲时使用 HalfFloat 和四重 MSAA，否则使用普通颜色缓冲；色调映射与输出颜色转换统一在最终合成执行。云开关保留相同输出链路，避免切换时整个场景亮度改变。F3 累加两个绘制阶段的统计。

进入云层后逐渐缩短可视距离，离开云顶后恢复基础远景雾。暂停或公告打开时云的漂移停止；换种子更新纹理内容并重置风偏移，保留云层开关。渲染目标仅在尺寸变化时重分配，图形上下文恢复后由渲染器重新上传资源。

「穿云起点」按距离环在当前坐标周围最多 ±3072 m 搜索耦合后的低谷，要求相机至少离地 80 m，并朝向附近较低的地形；路线未就绪或没有安全低谷时保持相机位置并提示。默认种子入口海拔 1720 m，地面约 1623 m。按 Space 连续上升可依次体验云下、约 91% 密度的云中与云上，Shift 可返回；仍使用自由相机，不代表车辆驾驶已经完成。

验证：73 项单元测试、20 项 Playwright 测试、TypeScript / Vite build 与 ESLint 通过。覆盖云场种子复现、周期边界、100 km 有限数值、原点重定位、暂停、开关、种子切换、目标尺寸与释放；浏览器检查真实穿云和能见度、暂停画面逐像素一致、图形丢失恢复后画面一致，以及开关确实改变云海画面。原有 30 km 实际飞行、道路／桥梁／生物群系和失败重试回归继续通过。十次换种子后 GPU 纹理保持 4 张（云场、场景颜色、场景深度及 Three.js 内部光照查找纹理）。

目视检查云下、云中、云上、开关对比及山体交界，调整观察方向、云顶明暗与低角度分层，避免初始视角正对山壁和明显的断裂条纹。页面和仓库公告升级为 0.1.2，0.1.1 原公告保留在历史。当前仍未实现 SFX / BGM，未声称完成音频试听。

性能抽测：Apple M5 Pro / ANGLE Metal / 1920×1080，默认视角连续飞行 10 秒约 60 FPS，p95 16.7 ms、最大 16.8 ms，末帧 289 分块、96 次 draw call、251694 个三角形；俯视云海覆盖大部分画面时另测 10 秒约 60 FPS，p95 与最大均为 16.8 ms。两次均无超过 50 ms 的主线程任务。另验证 1024×450 和 390×844 布局，以及模拟浮点颜色扩展不可用时仍正常渲染、无控制台错误。这些是单机短时及布局抽测，不代表跨浏览器、低端硬件或移动端支持。

## Phase 9 — 完成（v0.1.3）

新增 `SunSystem`、`SkySystem` 和 `SkyShader`，统一地形光照、云层着色、天空和雾的太阳方向及颜色。手动时刻从 0 到 1 对应太阳高度 45° 到 -3°；默认 0.7，即金色时刻的 11.4°。午后、黄金时刻和日落调色板平滑插值并复用颜色对象，太阳接近地平线时直射光连续减弱。没有自动昼夜循环。

天空渐变、太阳圆盘与光晕复用云海全屏合成，不新增天空网格。场景深度遮挡太阳；移除材质中的旧场景雾，在合成中统一处理深度雾和云中能见度，避免重复雾化。远景雾与天空使用相同的方向渐变，修复远山边缘的块状色差；浓云中的层片颜色随浸入程度融入同一雾色，消除明显明暗色带。地平线只裁剪太阳圆盘，光晕平滑延续，修复日落时的水平亮度断层，并通过地平线上下像素采样回归检查。

方向光使用 2048² PCF 阴影，覆盖相机附近约 3600 m 宽的灯光空间。灯光跟随相机，并在逻辑世界中按阴影 texel 对齐，再转换为局部坐标，减少移动及浮动原点造成的抖动。近中精度地形、道路和桥梁投射阴影，所有地形接收阴影；远景低精度分块不投射阴影以控制绘制成本。灯光、目标与阴影资源随 Game 销毁释放。

界面增加「光照时刻」「望向太阳」与「山体投影」。望向太阳只调整朝向并恢复探索；换种子保留时刻和投影开关。压缩控制面板间距，保留矮窗口滚动与收起入口。F3 增加光照阶段、太阳高度和投影状态，阶段更新为 `/ 09`。公告升级为 0.1.3，0.1.2 原公告完整保留在历史区。

测试覆盖太阳方向及色彩连续性、取样顺序独立、共享对象复用、100 km 灯光资源有界、原点重定位与释放。浏览器增加时刻往返画面一致、阴影开关确实改变画面与绘制次数、换种子保留设置、图形恢复及缩放测试；浓云像素采样检查明暗色带。修正云海截图测试的上升过冲和取景范围，确保比较区域实际包含云层。

验收：78 项单元测试、22 项 Playwright 测试、TypeScript / Vite build 和 ESLint 通过。最终光晕修复后，受影响的 4 项光照与云层测试再次通过。原有 30 km 飞行、反复换种子、Worker 故障重试和输入焦点回归继续通过；十次换种子后 GPU 纹理保持 6 张。

性能抽测：Apple M5 Pro / ANGLE Metal / 1920×1080，默认金色时刻、阴影开启，连续飞行 10 秒平均 60 FPS，p95 与最大帧间隔约 16.8 ms，无超过 50 ms 的主线程任务。末帧 289 个活动分块、188 次 draw call、662554 个三角形、6 张 GPU 纹理（较云海版增加阴影颜色与深度纹理）。这是单机短时抽测，不代表所有硬件或长期运行表现。

目视检查午后、金色时刻、日落、太阳朝向、桥梁长投影及云内／云上；390×844 窄屏控件与历史公告可滚动访问。模拟浮点颜色扩展不可用时正常回退并渲染，仅出现 Three.js 的预期扩展缺失提示，无运行错误。

## v0.1.4 — 交互与故障恢复检查

检查现有 UI、输入、暂停、错误恢复、公告与渲染流程，先用浏览器复现四类失败，再修复按键连发恢复移动、按钮焦点阻断 P / F3、故障期间仍可探索，以及长公告关闭入口离开视野的问题。生成失败、图形丢失和启动失败统一呈现不可交互的探索界面；重试或恢复后重新启用，保留显示偏好。另扩大滑杆操作区域、补充速度单位，并提高错误提示层级。自查发现窄屏调试面板遮挡设置后缺少方便的关闭入口，补上在滚动时保持可见的关闭按钮和回归测试。

版本更新为 0.1.4，旧公告完整归档。78 项单元测试、27 项端到端测试、build 与 lint 通过；本机 1080p 连续飞行抽测约 60 FPS。音频代码与资源仍不存在，未把声音测试标为通过；本轮不进入 Phase 10。复现、自查与验证范围见 [v0.1.4 检查报告](audit-v0.1.4.md)。

## v0.1.5 — 地貌、植被与双幅高速

按本轮需求增加高山雪岭、森林山谷、沙丘旷野、沙漠峡谷四种世界生成选项。森林采用低缓山体，沙丘采用受噪声扰动的长波沙脊，峡谷采用连续台地函数；岩层颜色按高度变化。沙漠生物群系使用独立暖干气候与雪线，顶点颜色保持有界和连续。金色时刻与日落的天空、云层和薄雾共用更偏橙色的调色板。

Worker 用分块种子生成原创松树与仙人掌，在最终地形三角形上计算根部位置，避开陡坡、雪区、路面与桥梁走廊。主线程使用两个实例批次，容量按活动分块及每块候选上限限定，复用缓冲，卸载时删除实例数据，浮动原点只移动批次位置。植被开关不重新生成地形。

统一道路配置传入路线、路面标线、路基、桥梁检测和 Worker。山路为双向两车道，高速为中央间隔 4 米的两条独立车行道，每向两车道；单幅沥青宽度可选 6 / 8 / 10 米，每侧另有 1.2 米路肩。高速坡度限制为 4%，不使用发卡弯，桥面和桥墩按车行道分别生成。自查移除高速桥内侧与连续中央护栏重叠的栏体。「道路视角」落在右幅上方。切换设置重新生成同一种子并回到起点，保留光照、云层、植被和调试显示偏好。

验证覆盖四种地貌的确定性与跨 LOD 接缝、不同宽度道路的真实三角形间距、双幅桥面承托、植物根部射线、原点重定位与资源释放，以及三种新增地貌各 100 km 的高速坡度和曲率。浏览器覆盖快速切换世界、道路宽度、植被显示、光照保留、窄屏选择控件与图形恢复；原有 30 km 飞行及回放检查一并通过。

100 项单元测试、30 项端到端测试和 1 项发布路径测试通过，build / lint 通过。页面与仓库公告升级为 v0.1.5，v0.1.4 完整归档。桥梁与中央护栏缓冲按车行道数量分配，避免山路模式承担双幅高速的额外容量。

性能抽测使用 Apple M5 Pro / ANGLE Metal、1920×1080、10 米双幅高速、阴影及云层开启，分别连续飞行 10 秒。森林末帧约 9856 株植被、184 次绘制、196 万三角形；沙丘 2670 株、186 次绘制；峡谷 890 株、171 次绘制。三者均约 60 FPS，p95 不超过 16.8 ms，未观察到超过 50 ms 的主线程任务，GPU 纹理稳定为 6。此结果只代表本机短时抽测；远处植被目前没有渐隐或独立树冠 LOD。

另修复 GitHub Pages 直接发布源码导致的白屏：Vite 使用相对资源路径，Actions 安装、检查并构建后只上传 `dist`，Pages 改用 workflow 部署。发布测试从 `/CLOUD-ROADS/` 实际加载样式、脚本、Worker 及 289 个分块。CI 软件渲染使用较小视口、关闭阴影并预留加载时间；本地完整视觉测试保留阴影。

## v0.1.6 — 桥隧、路灯与晴雨昼夜

桥梁增加共享批次的纵梁、栏杆扶手和立柱；普通道路增加金属护栏、立柱和近景沥青颗粒。山路中央使用黄色虚线，高速保留白色车道线与黄色内侧边线。路灯按种子和 720 米里程区间选择部分路段，每约 40 米布置，保留无灯区间。灯杆、悬臂和灯头共享实例几何，夜间发光；附近照明共用四个无阴影点光源，隧道照明也使用这个池。

隧道检测天然地面在全幅道路上方至少 11 米、曲率小于 0.0035 的完整覆盖段，生成 120–800 米洞段，排除桥梁及窗口边界不完整的候选。保留已经耦合的路基地形，沿道路坡度和横坡扫掠真实衬砌、开放洞口与山体覆盖网格，高速分别生成双洞。洞口覆盖高度向内部山体平滑过渡，侧墙含接缝及引导带，顶灯约每 24 米设置；覆盖区排除原地形植物。射线检查直路、真实弯道、两侧车道边缘、双幅高速的通行净空、顶部遮挡和原点平移。

新增晴天、多云、雨天、浓雾以及 07:00–22:00 手动时间范围，包含五个预设和连续滑杆。天空、雾与环境光共享状态，夜间增加程序化星点与月亮，雨天降低路面粗糙度。雨线在天空合成后叠加并读取场景深度，避免被背景覆盖；同一雨滴两端共享下落相位，暂停／公告不漂移。洞内依据道路局部坐标判断遮蔽，减弱云雾并隐藏雨。世界切换保留天气、时段和路灯开关。

新增「隧道入口」「灯光路段」观察入口以及 F3 天气、时刻、灯光和隧道遮蔽状态。测试覆盖新功能、旧版探索交互、长途流式加载、图形上下文恢复、雨线暂停和窄屏控件。v0.1.5 公告完整移入历史，当前版本升级为 v0.1.6。

110 项单元测试及 33 项浏览器检查通过，build / lint 通过。本机 Apple M5 Pro / ANGLE Metal、1920×1080 抽测：默认世界连续飞行、森林双幅高速雨夜、双洞内部各 10 秒，均约 60 FPS，p95 不超过 16.8 ms，没有超过 50 ms 的主线程任务。雨夜与隧道场景保留四个局部点光源，森林约 8400–9300 株植物，GPU 纹理为 6；此结果只代表本机短时抽测。

## v0.1.7 — 隧道细节、道路标识与服务区

隧道拱顶提高到 24 段，内壁采用随路线变化的平滑椭圆法线；洞门加厚，加入检修步道、设备管线、反光块、消防箱、绿色引导标记及通风设备。道路增加双向箭头、震荡槽、里程牌、限速牌和弯道提示。标牌使用一张原创像素字形图集，按实际牌面比例排字；标线以共同周期锚定世界里程，避免窗口回收时箭头移位。

服务区以 15 km 周期和 ±1.8 km 种子偏移确定目标，再在 ±600 m 完整窗口内优先选择坡缓、弯缓且地形高差较小的位置，相邻实际里程保持 10–20 km。山路右侧单场地，高速两侧独立场地；停车场地、连接匝道、主路护栏及边线开口同步生成，范围内排除桥隧。场地有休息建筑、补给棚、停车位、静态景观车、门窗、桌椅、垃圾桶与照明。

服务区地形随走廊快照进入 Worker。匝道先延续主路横坡，离开路肩后平滑过渡到停车场坡度，并为 4 m 地形三角形插值留出路基厚度。自查先复现并修复最高约 0.8 m 的匝道地形侵入、停车场基础与铺装共面闪烁、通道内灯柱、阔叶树索引混合导致的几何合并失败和路牌文字拉伸。森林新增阔叶树与林下灌木，干旱地貌新增灌丛，按连续疏密变化形成植被片区；四个共享实例批次随地形加载与回收。

「下一服务区」以独立有界路线逐帧定位，完成后俯瞰场地；暂停或公告暂停搜索，返回起点取消搜索。测试覆盖连续两处里程、取消和重放、24 种地貌／道路／宽度组合的实际匝道与地形三角形净距、场内通行、隧道设备净空、资源释放及原点平移。新版公告为 v0.1.7，旧版 v0.1.6 公告完整归档。

142 项单元测试、34 项浏览器检查、1 项发布路径检查以及 build / lint 通过。最终标线修复后补跑 4 项道路浏览器检查；另用 24 km 连续隧道窗口复现并修复通风设备实例容量不足。反复换种子后纹理稳定为 7 张，较上一版增加标牌图集。完整回归包含原有 30 km 飞行、图形恢复、天气和公告交互。

CI 首次运行中，24 km 隧道容量压力测试超过默认 5 秒时限，其余 141 项通过；仅为该压力测试设置 20 秒时限，保留完整场景和容量断言。

本机 Apple M5 Pro / ANGLE Metal、1920×1080、阴影开启，森林 10 m 双幅高速服务区俯瞰和隧道内部各静止观察 10 秒，均约 60 FPS，p95 不超过 16.8 ms，无超过 50 ms 的主线程任务。森林场景约 10811 株植物、304 万三角形、222 次绘制；洞内四盏局部照明开启，约 95 万三角形、219 次绘制。森林抽测关闭云层，洞内保留云层开关；此结果只代表本机短时观察，不代表所有硬件或长时间飞行表现。

## v0.1.8 — 起伏山路、垭口与可选视距

高山地形沿南北投影每约 32 km 按区域种子生成谷底、垭口高度与位置，通过连续高度和坡度衔接相邻山地。谷地两侧降低并放缓，垭口两侧保留较高山脊，附近局部噪声减弱。道路结合地形和长距离走势选择路线，反复上山与下山，山路和高速坡度仍分别限制在 6% 和 4%。其他三种地貌保留原有生成方式。

视距提供约 1.5 / 2 / 3 / 4 km，分别对应 169 / 289 / 625 / 1089 个活动分块。原地切换会重新规划，换种子和地貌保留选择；晴天雾和相机远裁剪面随之调整，天气与云中浓雾继续生效。最远视距的道路前后覆盖增至 5952 m，仍最多驻留 256 个路段；远景植被保留固定近景范围和实例容量，缩小视距释放多余分块。

新增「下一垭口」、当前路线阶段以及垭口和海拔牌；定位镜头对准同一个最高道路采样，支持暂停、公告暂停、取消和连续访问。服务区增加双坡屋顶、屋脊、檐沟、门窗框、廊架、种植池、充电桩、泵体软管与防撞柱，间距规则仍为 10–20 km。隧道增加瓷砖分区、带进气口与叶片的风机、吊架和出口引导牌。

自查定位到相邻折返路段的隧道覆盖侵入车道，改为按邻近道路距离收缩横向覆盖；去除与地面重合的覆盖面，使用共享顶点平滑法线，消除明显三角形条纹。洞门内侧不再与衬砌重叠。性能检查发现无变化的隧道随道路追加反复重建，现按完整洞段边界复用网格，并在生成覆盖层时直接共享顶点，减少重复处理。

新增测试覆盖 100 km 上下山与坡度、最远视距道路覆盖及返回重放、视距缩放回收、植被容量、连续垭口和取消重放。旧桥梁用例改用有桥的森林地貌，云层恢复检查对比恢复前纹理数量，室外灯开关检查保留洞内灯独立照明。公告升级为 v0.1.8，v0.1.7 完整归档。

155 项单元测试及 build / lint 通过。35 项浏览器用例完成回归，修正旧假设后补跑失败项与垭口入口；最终隧道缓存优化后再次通过 155 项单元测试、4 项天气／入洞／视距浏览器检查和 1 项 Pages 子目录发布检查。实景查看了低山谷、洞门、洞内、服务区和垭口。

本机 Apple M5 Pro / ANGLE Metal、1920×1080、默认金色时刻及阴影开启，各连续飞行 10 秒：2 km 平均约 59 FPS、4 km 约 57 FPS，p95 均约 16.8 ms；最大帧间隔分别约 83 ms 和 167 ms。两档分别记录到 2 次和 4 次超过 50 ms 的主线程任务，新增结构时仍有短暂卡顿。末帧活动分块分别为 289 和 1089，纹理均为 7 张；这是本机短时抽测。

## v0.1.9 — 可驾驶车辆、悬挂与驾驶仪表

按本轮用户要求，在既有无限世界上接入休闲驾驶；Scenic Director 的整体体验编排仍未完成。新增独立的 VehiclePhysics、DrivingSurface、VehicleMesh、DrivingCamera 和 DrivingSystem 模块，Game 只负责模式、更新顺序与生命周期。没有引入物理或 UI 框架。

车辆以逻辑双精度坐标运行，固定 1/120 秒步进，使用平面自行车转向模型、速度相关舵角、抓地限幅、坡度重力、阻力、制动和倒车。四轮分别采样接触高度，弹簧力和阻尼驱动车身升沉、俯仰与侧倾，车轮转向和滚动独立显示。三档悬挂改变弹簧与阻尼；R 在附近道路右侧安全驻车，保留本次行程。护栏和中央隔离带提供简化位置约束，服务区外侧开口放行，碰撞时按实际位移修正行程。

接触采样使用道路坡度和横坡平面，桥面不受桥下地形影响；服务区地坪与 7 m 宽匝道按实际铺装高度采样，其余区域使用耦合地形。雨天降低抓地。车模是原创低多边形敞篷车，包含方向盘、座椅、透明风挡、独立车轮与弹簧、制动尾灯，以及夜间和隧道自动开启的无阴影前照灯。

跟车、驾驶舱、引擎盖视角可用 C 切换，支持拖动观察、视角回正、50–90° 视野与 5–11 m 跟车距离。跟车相机在洞口前 14 m 缩近，限制洞内横向观察，按接触地面抬高；各视角与车模在浮动原点前后保持位置一致。车速仪表显示 km/h、行驶状态、行程与制动提示；进入驾驶自动收起面板，编辑设置、失焦、暂停和公告冻结车辆。更换世界、返回起点及景观定位会退出驾驶，保留悬挂和相机偏好。

按 TDD 先验证动力与接触，再接入渲染。实景自查调整过大的方向盘、风挡和仪表遮挡；图形上下文丢失时立即禁用驾驶入口，恢复后清除旧输入。v0.1.8 公告原文完整归档，当前公告与包版本更新为 v0.1.9。

167 项单元测试通过，新增覆盖加减速、转向倒车、四轮起伏与阻尼、悬挂调校、跨帧率一致性、桥面与匝道接触、湿路抓地、相机避让和浮动原点。40 项浏览器用例完成回归；更新旧版本标题断言和桥梁测试场景后，全部失败项与驾驶专项补测通过。最终风挡接缝及洞内跟车调整后，再次通过 12 项驾驶单元测试、2 项相机与隧道浏览器检查、1 项 Pages 子目录发布检查、lint 和 build。实景查看了三种视角、夜间隧道、制动尾灯和窄屏仪表。

本机 Apple M5 Pro / ANGLE Metal、1920×1080、默认种子与金色时刻、阴影开启，各连续驾驶 10 秒：2 km 视距约 60 FPS，p95 16.7 ms、最大 16.8 ms，无超过 50 ms 的主线程任务；4 km 约 59 FPS，p95 16.8 ms、最大 133.4 ms，记录到一次 147 ms 主线程任务。末帧活动分块为 289 / 1089，纹理均为 7 张。最远视距追加内容仍有短暂卡顿；这是本机短时抽测。可用 `npm run benchmark -- --drive` 和 `--drive --far` 复测。

## v0.1.10 — 山坡细节与连续植被

本轮针对山体单调、树木稀少与远坡空白继续优化。候选覆盖完整 256 m 分块，消除原有边缘空带；每块 20×20 树木／岩石候选及一层地被候选，以种子噪声形成林斑，按坡度、海拔、雪线与生物群系选择松树、阔叶树、仙人掌、灌木、草和碎石。近处使用分枝与错层树冠的原创低多边形几何，尺寸、纵向比例和色调有变化；远处每个全局 2×2 候选单元固定保留一个，以简化树冠延续山坡轮廓。实际是否生长仍取决于对应 LOD 的地形与坡度。

植被根部按实际地形三角形采样，包括低精度分块边缘的连接三角形；道路避让增加树冠半径，保留隧道与服务区避让。完整树木范围为四圈分块，地被三圈，远景随所选视距加载；共九个实例批次，容量按各层候选数有界。增加与删除只上传变动范围，浮动原点通常只平移批次，超过 16 km 再重写实例锚点。压力测试复现并修复双向往返时先添加后回收造成的容量峰值，改为先统一移除待更新实例。

山体增加中小尺度山脊起伏，保留谷底与垭口的宏观走势。草地增加噪声色差，山体材质加入世界坐标程序纹理和近景法线细节，屏幕覆盖过小时衰减细颗粒；纹理周期与原点重定位对齐，无外部图片资源。实际浏览器编译发现并修复 GLSL 保留变量名。

新森林地形引出三种山路宽度下的服务区回归：停车坪靠近回头弯并与主路重叠。选址现在检查整块场地及净空边界与道路线段的相交，排除冲突候选；保留真实铺装与地形三角形间距断言，24 种地貌／道路／宽度组合通过。F3 增加树冠、远景树冠与地被实例数，当前公告升级为 v0.1.10，v0.1.9 原文完整归档。

170 项单元测试、lint 与 build 通过。41 项浏览器用例完成回归，其中两项测试时序问题修正后补测通过：视距切换需先等目标分块刷新，故障注入需限定目标种子，避免误伤尚在生成的旧世界。最终枝干与树冠衔接调整后，植被、公告、故障恢复共五项浏览器检查通过，包含 4 km 树冠加载、缩小视距回收、关闭／重开画面一致和上下文恢复。完整回归还覆盖两条方向的 30 km 流式飞行、驾驶、桥隧、服务区与天气。Pages 子目录发布检查通过；最终实景查看了新版公告、树林俯瞰和驾驶路景，无浏览器运行或着色器错误。

本机 Apple M5 Pro / ANGLE Metal、1920×1080、默认种子与金色时刻、阴影开启，各连续驾驶 10 秒：2 km 视距平均 60 FPS，p95 16.7 ms、最大 16.8 ms，无超过 50 ms 的主线程任务；4 km 平均 59 FPS，p95 16.8 ms、最大 133.3 ms，记录到一次 138 ms 主线程任务。末帧植被实例分别为 35980 / 74678，活动分块 289 / 1089，纹理均为 7 张。植被丰富后本机短时抽测维持相近帧率，但最远视距追加内容仍可能卡顿，不代表其他硬件与长期运行表现。

首次 CI 发布中，纯软件渲染持续绘制密集树木导致 289 个分块未能在 90 秒内加载完毕。Pages 检查改为仅在 CI 加载期间隐藏植被，加载完成后重新显示、检查树冠数量并截图触发实际渲染；仍检查全部资源、Worker、活动分块与控制台错误。本地强制 SwiftShader 的完整发布检查在 48.5 秒内通过，默认产品画面及硬件加速视觉回归不变。

## v0.1.11 — 路线风格、隧道与无限生成

路线风格独立于地貌、道路类型和宽度。自然路线保持原有地形引导；蜿蜒路线在平缓地貌也主动规划折返，组间冷却约 1.2–2.1 km，组内保留露天横向段；高速沿缓弯推进，不进入急折返序列。峡谷挂壁使用连续高度场，沿山腰平台布置道路，左侧下降到深谷，右侧抬升为高崖，远侧形成对岸山体；路段候选同时追踪峡谷平台的横向位置与高度。主线程和 Worker 使用同一风格及种子参数，地貌决定配色与植被。

道路箭头长度约 8.3 m、间距 60 m，UV 以 1560 m 公共周期锚定，避免滚动窗口改变箭头、虚线或震荡槽的位置。山路两侧均布置里程、限速与弯道提示，反向车道翻转弯向标识。隧道加强带与排水槽使用共享几何批次，洞口加入黄黑标记。实景发现折返两侧被覆盖、洞门外缘呈大块平墙，改为蜿蜒组间缓弯生成隧道，覆盖层向洞内渐退并复用地表纹理；双向山路与双洞高速的车道净空射线检查通过。

道路每跨约 2048 m 保存完整控制点，最多 64 个内存检查点，包含折返阶段与下一组触发里程；后退到已卸载路段时从覆盖窗口之前的检查点重建，景观搜索通过 fork 复用检查点。分块规划在道路重放期间仍更新目标区域、回收远处分块并丢弃过期 ready 结果；视线方向变化时重排待生成区域，保留道路附近的细节级别。正在执行的 Worker 仍正常完成，仅丢弃过时结果。

按 TDD 验证连续折返、峡谷横剖面与跨 LOD 边界，新增路线在山路／高速各生成 100 km，检查接缝、有限数值、坡度、曲率及临崖比例；最长视距下往返 40 / 100 km 重建同一路段窗口，检查内存容量与景观搜索复现。服务区扩展到蜿蜒森林和挂壁沙漠，两类道路和三种宽度均通过实际铺装、匝道与地形三角形净空检查。公告升级为 v0.1.11，v0.1.10 原文完整归档。

193 项单元测试、42 项浏览器测试、Pages 子目录发布检查、lint 与 build 全部通过。浏览器覆盖新风格切换、4 km 视距、驾驶与上下文恢复、服务区定位和换种子保留选择，原有两种方向的 30 km 飞行、桥梁、天气、暂停及公告交互继续通过。实景查看了森林折返、沙漠挂壁驾驶、隧道内外，未发现运行或着色器错误。

本机 Apple M5 Pro / ANGLE Metal、1920×1080、默认种子与金色时刻、阴影开启，分别抽测 10 秒：自然路线 2 km 视距连续驾驶平均 60 FPS，p95 16.8 ms、最大 16.8 ms；4 km 驾驶平均 59 FPS，p95 16.8 ms、最大 133.3 ms，记录到一次 141 ms 主线程任务。4 km 挂壁路线连续飞行平均 60 FPS，p95 16.7 ms、最大 16.8 ms。仅按 W 的挂壁驾驶会在前方弯道碰到护栏，因此没有将该次停靠期间的帧率计作连续驾驶性能。最远视距追加内容仍有短暂卡顿；这些是本机短时抽测，不代表其他硬件和长期表现。

## v0.1.12 — 缓坡高速与地形过渡

高速从山路候选评分中独立出来。优先追踪远处谷线；没有谷线的地貌使用 12 km 尺度的方向变化，采样前方 400 / 1000 / 1800 m 地形决定缓慢爬升或下降。方向逐步收敛，每段转角最多 3°，对应最小半径约 1222 m；坡度上限 3%，每 96 m 最多变化 0.2 个百分点。盘山风格不会覆盖高速约束。山路保留原有折返生成逻辑。

检查发现只降低道路坡度会导致挂壁高速无法跟上旧峡谷的高差，临崖比例下降。高速的高山与峡谷山地组因此从约 32 km 拉长至 64 km，谷线横向变化与峡谷弯曲也同步舒展。主线程、Worker 和默认道路构造使用一致的道路类型参数；谷地、山口及地形边界连续。峡谷横剖面增加双层岩壁、随里程变化的台地、谷宽、弯曲冲沟与对岸高度，保留沿线平台。

道路在向前重访远处已缓存窗口时也会寻找覆盖区之前的检查点，跳过中间无需显示的路段；正常前进继续增量生成。TDD 复现原先重访 100 km 需要重算 629 段的问题，新实现控制在 160 段以内，包含完整 4 km 视距的前后覆盖。40 / 100 km 双向往返与 300 km 缓存淘汰后返回均重建相同控制点，最多保留 64 个检查点，不写入运行时存档。

新增四种地貌、三种路线风格各 100 km 的高速测试，检查坡度、曲率、方向摆动、接缝和路线绕行程度；另验证 300 km 高度连续、跨 LOD 接缝与主线程／Worker 地形一致。原有服务区、桥隧、道路接触及山路测试继续通过。页面升级为 v0.1.12，v0.1.11 原公告完整归档。

实景自查发现挂壁高速横向追踪滞后，局部偏入岩壁并产生突兀的隧道覆盖隆起。挂壁选线改为追踪前方 700 m 的平台位置及高度，在高速曲率、坡度约束内更及时修正；三个种子各 100 km 的道路中心与引导平台横向偏差小于 30 m、高度偏差小于 65 m。复查洞口与山腰实景后确认该处隆起消失，行车道畅通。另修正驾驶浏览器测试的时序：先等待车辆归零，再按油门，避免沿用上次驾驶的速度读数。

补测复现远处定位完成时仍有一帧沿用旧道路就绪标记，导致提前读到旧服务区里程。服务区／垭口定位及返回起点现在立即清除就绪标记，加载完成后再开放后续定位；单元测试覆盖重定位当帧和未就绪时的重复请求。

210 项单元测试、43 项浏览器检查完成回归，最终修复后高速、路线风格、视距／垭口和服务区相关补测通过。发布子目录检查、build 与 lint 通过。实景检查覆盖高山自然高速和沙漠挂壁高速驾驶，未出现页面或控制台错误；v0.1.11 历史公告与原文一致。

固定生产预览下，Apple M5 Pro / ANGLE Metal、1920×1080、高速、4.10 km 视距连续飞行 10 秒，约 59 FPS，帧间隔 p95 为 16.8 ms、最大 199.9 ms，未记录到超过 50 ms 的主线程任务。目标 1089 块，结束时活动 1056 块、待处理／排队 4 / 57，飞行约 1.15 km，仍在持续加载。此结果只代表本机短时抽测，仍有单帧延迟。开发环境中一次相同参数报告为 2.05 km，具体触发原因未确认，不纳入远景性能数据；固定生产构建复测确认 4.10 km 生效。仅按住油门的驾驶抽测因触碰护栏减速至近乎停止，不作为持续驾驶或流式加载性能依据。

## v0.1.13 — 高架盘山、地形预加载与驾驶视角

桥梁判定改为路面到自然地面落差 >5 m，取消旧深度、最短长度和最长跨度限制。双幅高速以两幅中心的最大落差判断；短桥也保留。桥段记录两端是否被窗口裁切，裁切处不生成桥台、不恢复填方；真实桥头在相邻道路采样内衔接路基。桥侧山体仍切至纵梁以下，避免只取消填方后桥面穿山。服务区仅切出自身的道路路基区间，不再整段拒绝长桥。

高桥使用底宽顶窄的双柱、每 38 m 高度的横系梁、盖梁与支座，基础四角采样后下埋。支撑点按绝对道路里程每 48 m 布置，落差超过 80 m 的深谷段放宽至 96 m，与滚动窗口无关；高墩随高度加宽，桥面补充伸缩缝，灯杆移入桥面范围。「高架盘山公路」沿原连续山地引导线增加深谷与山脊交替，山路横向摆动增强，高速保持独立缓弯与缓坡约束。箭头间距由 60 m 改为 2000 m，UV 公共周期为 78000 m，覆盖虚线、排水带接缝和震荡槽，局部坐标保持有界。

附近地形优先进入 Worker 队列，空闲时准备沿朝向提前一圈的分块与 LOD 替换数据。最多缓存 128 项，跨区进入上传队列，转向和瞬移清除无关缓存；仍以每帧最多两块、2 ms 的启动预算上传，旧 LOD 保留到替换就绪。驾驶时使用车辆行进方向，倒车时反转，自由探索使用视线方向。道路覆盖扩大至最远前后 6208 m，为预加载窗口保留完整路基和结构检测余量。缓存只在内存中，不写入仓库或构建产物。

道路隔离墩改为宽底窄顶的成形截面，外围护栏使用波形金属板及独立立柱，复用实例批次；服务区开口与桥隧避让保持原规则。路肩增加排水带接缝，沥青增加低对比度轮迹。峡谷岩壁横剖面增加沿里程变化的岩柱和凹槽；三向混合程序纹理减轻陡壁拉伸，按屏幕采样范围衰减颗粒及岩层细节，隧道覆盖层共用同一材质。

驾驶镜头增加 -10 至 +35 cm 高度调整，FOV、视角及跟车距离在车辆冻结时即时预览。第一人称位置跟随车身，俯仰与侧倾经过平滑，避免位置滞后穿进车体；跟车位置沿视线检查山体并缩近，隧道内保留高度与距离限制。实景发现方向盘过低、后视镜缺支撑，已调整驾驶眼位、方向盘并补齐镜座和镜面。镜面为材质表现，没有实时后方画面。

TDD 先复现预加载缺失、静止调节不更新、第一人称突发旋转和跟车视线被山脊阻挡，再实现修复；补测六种道路／风格组合各 100 km 的最远视距与预加载覆盖。页面升级 v0.1.13，v0.1.12 公告完整归档。

最终验收：228 项单元测试、44 项浏览器 E2E、1 项 Pages 子路径检查全部通过，TypeScript / Vite build 与 ESLint 通过。新增覆盖 5 / 5.01 m 阈值、短桥与 8 km 开放桥、服务区切分、100 km 重叠窗口、高墩位置与尺寸、真实弯曲桥面净空，以及 2 km 标识在远里程回收后的连续性。实景检查三种驾驶视角与沙漠高架桥，未出现页面或控制台错误；v0.1.12 公告与原版逐字比对一致。

Apple M5 Pro / ANGLE Metal，1920×1080、4.10 km 视距，各连续飞行 10 秒：高架高速 60 FPS，p95 16.7 ms，最大 16.8 ms，无超过 50 ms 的主线程任务；自然高速 59 FPS，p95 16.8 ms，最大帧间隔 266.6 ms，仍出现一次 280 ms 主线程任务。两者结束时可见地块均为 1089 / 1089、待加载为 0 / 0，分别缓存前方 56 / 57 块。高架场景载入 124 个支撑、最高落差约 348 m；截图检查的沙漠山路最高约 368 m。短测仅代表当前机器与路线，尚未消除自然路线的单帧峰值；测量和截图均放在临时目录。

首次远端部署的 Pages 检查在软件渲染完整树林后超过 120 秒。本机强制 SwiftShader 复测耗时约 84 秒，确认软件路径明显慢于硬件加速；CI 发布冒烟检查改用 320×240 画布和 180 秒总预算，保留 289 个地块、植被数量、实际绘制、截图及资源路径断言。调整后本机软件渲染检查约 66 秒通过；本地完整 E2E 和游戏默认分辨率不变。

## v0.1.14 — 步行跑跳与分级高桥

新增 WalkingPhysics 和 WalkingSystem，使用逻辑世界坐标及 120 Hz 固定步长。WASD 行走 2.2 m/s，Shift 跑步 4.8 m/s，E 疾跑 8 m/s，地面／空中加速度分别为 18 / 5 m/s²；速度向量插值消除斜向加速优势。Space 以 6.4 m/s 起跳，重力 18 m/s²，最大跳高约 1.11 m，必须松开并再次按下才能下一次起跳。分轴移动、脚底五点采样限制高台阶和空中爬墙，桥下采样排除头顶道路，桥底及隧道有简化顶头约束。

第一人称眼高 1.65 m，视野 70°，近裁面 0.08 m，脚步仅加入 25 mm 的轻微起伏。步行、驾驶和自由飞行互斥；暂停、公告、失焦、编辑控件和道路未就绪时冻结，恢复前清除旧输入。R 在最近道路安全点重新站立；换世界、回起点、景观定位退出当前模式。步行状态和速度有独立仪表，F3 显示位置与落地状态；窄窗口顶栏采用两列按钮。

RoadProtection 共享地面护栏规则，平缓路段按种子、左右侧及 160 m 里程段留出缺口，桥梁、隧道和中央隔离带保留防护。车辆与步行使用同一规则，桥下不碰撞悬空护栏。补测发现横坡高侧的跳跃会提前超过按道路中心计算的护栏高度，修正为局部路面高度后回归通过。

桥梁按路面到自然地面落差分级，基础下埋不计入：≤50 m 保留 48 m 支撑间距，>50 m 使用 96 m，>100 m 使用 192 m。支撑按绝对里程锚定，裁切边缘不增加假桥台；每段高度缓存只保留活动窗口。高桥采用双柱与交叉斜撑钢护栏，超高桥加粗墩身、基础、盖梁，横系梁间距缩至 28 m，并使用红色钢护栏。护栏增加一个实例批次，使用逐实例颜色，滚动和原点重定位时复用缓冲。

公告升级 v0.1.14，v0.1.13 原文完整归档；所有测试运行数据与截图放在临时目录，不保存玩家状态。

远距离高速服务区补测复现跳跃键在同一帧内按下并松开时被轮询漏掉的问题。WalkingSystem 保留按下事件至下一次物理更新，暂停、失焦、回正和模式切换清除待处理事件；回归覆盖同帧点按、长按不连跳、浮动原点后的相机／脚底坐标及更换世界。

238 项单元测试通过，48 项浏览器用例完成回归（47 项全量通过，修复短按跳跃后 4 项步行专项全部通过，含新增远距离用例）；TypeScript / Vite build、ESLint 与 Pages 子路径发布检查通过。实景检查红色钢护栏、超高墩大跨度桥、桥面步行、地面开口与窄屏操作，无页面或控制台错误；v0.1.13 公告与原版逐字一致。

使用 CI 的 320×240 画布并强制 SwiftShader，再次通过完整 Pages 检查，耗时约 1.3 分钟；保留 289 个地块、树林恢复、实际绘制、资源路径和截图断言。

Apple M5 Pro / ANGLE Metal，各抽测 10 秒：1440×900 沙漠高桥疾跑约 60 FPS；1920×1080 从地面缺口进入山野后疾跑约 60 FPS；1920×1080、4.10 km 视距高架高速连续飞行约 60 FPS。三次 p95 为 16.7–16.8 ms、最大均约 16.8 ms，未记录到超过 50 ms 的主线程任务。远景末帧载入 1089 / 1089 地块、82 个桥梁支撑、最高落差约 348 m，纹理 7 张；山野实测离开道路约 49 m 后继续前进约 79 m，R 可返回道路。这些是特定路线的本机短测，未解决上一版记录的自然路线远景单帧峰值。

## v0.1.15 — 区域地形、自然谷地与连续桥梁

区域噪声改变森林起伏与盆地、交错沙丘的高度和权重、沙漠台地与冲沟、高山谷宽和侧脊，以及峡谷宽度、岩壁层次与深谷起伏。相邻山地共用连续谷底海拔，峰高同时受上山／下山长度限制，避免增大随机变化后超过高速 3% 坡度及谷线追踪约束。沙丘采用固定方向的两个域混合，避免坐标相关旋转在远处产生越来越密的波纹。种子确定性、LOD 接缝和主线程／Worker 一致性保持原规则。

路基影响半径从 160 m 缩小至 64 m，土方过渡宽度不再随几百米落差无限扩大；所有填方相对自然地面限制在 5 m 以内。桥梁检测从车行道中心扩展到两侧路基边缘，避免中心落地而路肩悬空时仍造高土坡。主桥不再因服务区而切断：场地角点、中心或匝道有超过 5 m 落差时，整处场地使用架空平台、梁柱、基础及钢护栏，只保留通行净空切坡，不抬高下方谷地。服务区间距仍为 10–20 km，原有完整窗口选址与缓存逻辑保持不变。

BridgeDeck 沿道路横坡和纵坡共用连续六边形截面，形成顶面、斜腹板、底板和闭合端面的箱梁，消除短方块在弯道处的缝隙与突起；弯道按约 24 m 添加横隔梁。支座位于箱梁下方，与盖梁相接，桥墩保持竖直并延伸到倾斜盖梁底部。自查用射线复现两侧支座未跟随横坡的问题，改为使用实际桥面坐标架；箱梁缓冲仅扩容时替换并释放旧几何，相同容量复用。主桥为一个连续网格和五个实例批次，服务区增加混凝土支撑与钢护栏批次。

混凝土采用程序化模板缝、三向混合色斑、细孔、微小法线起伏与水痕，细颗粒按屏幕覆盖范围衰减；使用逻辑坐标的周期原点兼容远处重定位，不引入图片资产。架空服务区与匝道护栏共享渲染及物理边线，只有真实匝道与主路相接的窗口开放，平台和匝道底面加入步行顶头约束。修正宽幅高山匝道被地形三角形局部覆盖的问题；垭口标识在山口附近选择不被隧道覆盖的道路采样。

公告升级 v0.1.15，v0.1.14 原文完整归档。算法调整会改变旧种子地形及设施位置，不保存玩家状态或未发布算法兼容层。

245 项单元测试、49 项浏览器用例、TypeScript / Vite build 和 ESLint 全部通过。新增回归覆盖路基最大填方、悬空路肩架桥、区域高差、150° 弯桥连续表面与底板、倾斜桥面的支座连接、缓冲扩容回收、架空服务区护栏与底部碰撞，以及最远视距下的远距离服务区、重定位和图形恢复。服务区继续检查 36 种地貌／风格／道路／宽度组合的实际地形三角形与铺装净空；v0.1.14 页面公告及 CHANGELOG 正文均与提交前原版逐字一致。

Pages 子目录资源发布检查通过；生产画面复查高山谷地、交错沙丘、红色护栏高桥、箱梁近景与底面、桥面步行，以及 14.91 km 的架空服务区，无页面或着色器错误。截图与临时验证脚本均放在临时目录。

按 CI 的 320×240 画布强制 SwiftShader 软件渲染，完整 Pages 检查约 1.1 分钟通过，保留全部 289 个地块、恢复树林后的实际绘制、截图和资源路径检查。

首次远端发布在真实双洞高速净空射线测试超过 Vitest 默认 5 秒预算，其他 244 项单元测试通过。该用例的完整模型射线检查改用与长窗口结构测试相同的 20 秒预算，保留所有道路采样、车道边缘及碰撞断言，不改变产品运行代码。

Apple M5 Pro / ANGLE Metal、1920×1080、4.10 km 视距，高架高速连续飞行 10 秒约 60 FPS，p95 16.8 ms、最大帧间隔 33.4 ms，无超过 50 ms 的主线程任务。末帧活动分块 1089 / 1089、待加载 0 / 0、预加载 56 块，88 个桥梁支撑、最高落差约 371 m、7 张纹理。这是特定路线的本机短测，不代表所有硬件或长时间运行表现。

## v0.1.16 — 路旁花草、自选坡度与五档蜿蜒

路线选项改为 0–5：0 锁定方向、曲率和横坡，1 保持山路普通缓弯及高速大半径选线，2–5 逐步增加折返组规模、缩短弯间及组间间隔。5 档转角约 150°、弯长 160 m，以连续发卡弯为主。两种道路均使用所选档位，不再保留挂壁／高架盘山专用高度场，天然山形与蜿蜒程度独立。

最大坡度以 0–40% 显示，内部使用比例，同时约束升降；0% 为水平，默认 6%。道路提前采样地形并渐变坡度，上限不等于强制沿途保持该坡度。全直道可起伏，也可与 0% 组合成水平直路。设置重新生成世界，换种子和图形恢复保留选择，不保存运行时玩家状态。

草叶和野花使用独立种子、4 m 候选单元及斑块密度，按实际地形三角面落根，避让路面、隧道、服务区、陡坡、干旱和雪区。新增两个实例批次，仅显示玩家附近 3×3 地块细节，随移动、重定位和回收更新。花朵包含白、黄、淡紫花瓣和花芯，沿用现有顶点色材质。森林起点驾驶实测附近 654 丛细草、158 簇野花；检查了跟车、步行近景及连续折返高速画面。

极端组合检查发现，连续发卡弯会让宽幅高速服务区选址失败或匝道穿地。道路与服务区共用确定性里程计划，在目标前约 1 km 至后约 800 m 留出缓弯缓坡；其余路段保留所选蜿蜒程度。新增 40% 宽路服务区、桥面与隧道净空，以及四轮爬坡和手刹测试；桥下地形射线改与实际天然三角面比较，允许必要净空切坡，仍禁止桥下填方隆起。

273 项单元检查通过，覆盖五档弯道密度、0–40% 上下坡、0 弯道恒定方向、最远视距下 100 km 重建及花草回收。浏览器专项检查覆盖六种选择、水平直道驾驶、种子与图形恢复，以及高桥、远处架空服务区。视觉检查修正新帮助文字遗漏面板小字样式的问题。v0.1.15 公告原文完整归档，当前公告升级 v0.1.16。

完整浏览器回归首轮 49 / 50 通过；唯一失败是旧挂壁起点不再生成百米高桥，改用已验证存在约 291 m 高桥的森林场景后，该用例通过，保留桥上护栏、步行／驾驶切换和图形恢复检查。共 50 项浏览器用例通过。旧版强制挂壁和固定高速 3% 的断言替换为新选择的对应约束，未删除桥面净空与自然地形保护检查。

Apple M5 Pro / ANGLE Metal、1920×1080、4.10 km 视距与 5 档高速，各抽测 10 秒。驾驶约 60 FPS，p95 16.7 ms，最大 33.3 ms；该脚本只按油门，弯道触碰护栏后减速，不能代表持续循线驾驶。连续飞行约 59 FPS，p95 16.8 ms，最大 49.9 ms，记录一次 55 ms 主线程任务。飞行末帧活动分块 1089 / 1089、待加载 0 / 0、预加载 54 块，附近 1988 丛细草、450 簇野花；纹理仍为 7 张。测量仅代表当前机器和路线。

Lint、生产构建、原生 Pages 项目路径检查均通过；按 CI 的 320×240 画布强制 SwiftShader 软件渲染，Pages 检查约 1 分钟通过，保留全部 289 个地块、恢复树林后的实际绘制及资源路径检查。

## v0.1.17 — 峰林云桥与山路十八弯

新增草甸、红岩、喀斯特三种确定性高度场，生态配色与树木密度同步适配。爬升模式在控制点中保存当前方向、谷底与目标高度，每次选取 50–2000 m 范围内的净爬升后回落，随检查点重放；最大坡度仍为 0–40%，服务区附近逐步降坡。

超过 200 m 深的桥段使用约 384 m 间距的 H / A 形桥塔和双侧扇形拉索，完整短深谷没有固定塔位时在跨中补塔。低桥交替使用圆形双柱与实体墩，高墩保留渐收双柱。基础按实际尺寸采样地面，拉索检查道路净空；实例批次支持版本更新、浮动原点与销毁。

服务区按地貌与站点轮换木屋、现代平顶、庭院三种建筑，入口和平台布局保持一致。路侧加入反光导向柱、陡坡慢行与低速标牌，新增近景紫色穗状花。自查修复无效爬升范围阻止自然模式应用、短深谷漏塔、宽基础取样不足、反光柱条带被遮住和喀斯特桥梁视角被前方山脊遮挡。实拍发现新峰体过尖，改用圆滑峰顶与孤立峰林轮廓，减少细网格上的薄片状尖刺。

292 项单元检查、TypeScript / Vite 生产构建及 ESLint 通过。新增检查覆盖 50 / 2000 m 爬升边界与 40% 坡度、70 km 反复升降、100 km 检查点重建、200 m 桥型阈值、短深谷补塔、宽基础四角落地、弯道拉索净空，以及三种新地貌上的宽幅陡坡高速服务区。浏览器实拍核对三种地貌、三种服务区、斜拉桥、驾驶和矮窗口设置布局，未记录脚本或图形错误。v0.1.16 公告和 CHANGELOG 历史段落与旧提交逐字比对一致。

完整浏览器回归 50 / 52 首轮通过；两处测试断言分别误写坡度小数位、在 R 回到道路后读取了尚未刷新的旧坐标。修正为现有两位小数格式，并等待回正坐标更新后检查公告暂停，两项复测通过，合计 52 项通过，未放宽步行冻结、桥上行走或图形恢复的要求。

Apple M5 Pro / ANGLE Metal、1920×1080，喀斯特、4.10 km 视距、10 m 双幅高速、5 档、25% 坡度与 300–900 m 反复爬升，各抽测 10 秒。驾驶约 59.8 FPS，p95 16.7 ms，最大 50 ms；脚本只按油门，触碰弯道护栏后减速，不能代表持续循线驾驶。连续飞行约 58.8 FPS，p95 16.8 ms，最大 50 ms，记录一次 60 ms 主线程任务；末帧 1089 个活动分块、待加载 0 / 0、30 座桥塔、487 根拉索，纹理保持 7 张。以上仅代表该机器与短时路线。

Pages 项目路径检查在原生 GPU 与 CI 尺寸的 SwiftShader 软件渲染下均通过；后者约 60 秒完成，核对脚本、样式、地形 Worker、289 个地块与恢复后的实际植被绘制。

## v0.1.18 — 载具车队与雨雾

载具配置统一描述十种车型的整车长度、车身、车轴、质量、功率、驱动力、制动、重心、悬挂和驾驶舱视点。5 / 8 m 货车、15 / 20 m 半挂、8 / 12 m 客车、两轮摩托车与原有跑车、轿车、SUV 共用 120 Hz 固定步长；五档悬挂按车型修正固有频率与阻尼，轮胎独立采样路面，车身随接触产生俯仰与侧倾。质量为游戏中含载荷的调校质量，没有载重编辑器。

半挂通过牵引座跟随，挂车独立计算航向、坡度和轮胎位置，倒车铰接限制约 77°，可前进回正。统一模型和接触点的倾斜坐标变换，修复长挂车在 40% 坡道上的轮胎悬空；刹车直到速度归零后才允许下一步倒车。整车按纵向分段检查道路与服务区护栏，碰撞恢复车头、挂车及轮胎姿态，允许向安全方向脱离；换车检查附近完整车身占地并保留本次行程。

模型采用共享基础几何与分车型车身，补充倾斜风挡、轮拱、座椅、后视镜、货厢锁杆、反光灯、挂车支腿和客车后舱。跟车镜头按长度调整距离并围绕整辆半挂，长车进入隧道临时使用驾驶舱；驾驶舱高度受车顶限制。实拍复查修正车头尺寸超限、半挂转动观察时车尾出画，以及货厢后门与车身共面闪烁。

天气增加小雨、风雨，平滑过渡云量、雨量、风速、能见度和日照。雨线在浮动原点下保持世界定位，遮挡与隧道遮雨保留；地面湿度逐渐积累、缓慢消退，关联道路粗糙度和抓地力。雾气浓度可调 50–200%，按径向距离与视线高度渐隐，不能超过当前地块可见边界；暂停时的主动选择即时预览，环境动画保持冻结。

312 项单元检查、生产构建及 ESLint 通过。新增测试覆盖十种车型 × 五档悬挂的坡面静止、模型尺寸和资源回收、重车加速、湿路制动、半挂倒车回正、40% 坡面轮胎接触、不同帧率一致性、摩托车侧倾、整车护栏和长车隧道视角。浏览器逐辆实际起步、刹停、换车并验证世界与图形恢复，实拍检查十种外观和驾驶舱。v0.1.17 页面公告与 CHANGELOG 归档逐字核对一致。

完整浏览器回归首轮 53 / 54 通过；遗漏更新的旧公告标题断言修正后通过，合计 54 项通过。模型细化后重新执行公告与新增载具专项 3 项，全部通过。Pages 项目路径检查在原生 GPU 与 CI 尺寸 SwiftShader 软件渲染下均通过，软件检查约 60 秒。

Apple M5 Pro / ANGLE Metal、1920×1080、喀斯特、4.10 km 视距、10 m 双幅高速、1 档弯道与 6% 坡度，驾驶 20 m 半挂并开启风雨，连续抽测 15 秒约 59.5 FPS，p95 16.7 ms，最大帧间隔 100.1 ms，记录一次 104 ms 主线程任务。末帧约 41 km/h，1089 个活动地块、待加载 0 / 0、893 次绘制、7 张纹理；无脚本或图形错误。该短时结果不代表所有路线、软件渲染或其他硬件的持续性能。

## v0.1.19 — 车灯、雨刮与季候地形

车灯提供自动、关闭、近光、远光及 L 快捷键，灯罩自发光与实际光束同步切换；自动模式设明暗滞回，刹车灯独立。雨刮提供自动、关闭、间歇、低速、高速及 B 快捷键，按雨量和车辆是否进入隧道调整，关闭完成当前周期后停车归位。间歇切连续取消等待；设置、暂停、公告和失焦冻结动画，摩托车禁用雨刮并保留其他车型的选择。

九种四轮或多轮载具的双雨刮由各自前风挡位置、倾角及尺寸生成。120 个实例水滴位于真实玻璃平面，随雨量积累、按刮片扫过的角度及半径清理，雨后缓慢消退；轻量着色器提供边缘和高光，没有屏幕空间折射。无需为驾驶舱、跟车、侧看和隧道临时驾驶舱额外覆盖屏幕。换车释放刮片、材质及实例资源。

五档弹簧增加 70–130% 阻尼倍率，不改变弹簧频率、行程及静态车高，面板展示车型频率、行程与轮端压缩。加速／制动与转向共用摩擦预算，制动上限留有转向余量；半挂将车头及挂车接地轮胎按等轮端权重计入制动抓地力。仍是无侧滑速度状态的平面车辆近似，没有动态轴荷或完整轮胎模型。

新增火山高地、冰蚀苔原和秋色丘陵，分别使用宽缓火山锥／凹顶、宽谷侧坡、起伏缓丘高度函数。生态层保持权重归一，提供深色岩地、苔原低雪线、秋色土壤和树冠。秋叶树有独立近远实例批次，保持种子确定性、远景子集与回收；不额外存储玩家状态。

322 项单元测试、构建与 ESLint 通过，覆盖灯光模式、雨刮周期／归位／帧率／暂停、玻璃积水与清理、独立制动灯、阻尼倍率、湿路联合抓地、挂车混合路面、新地形连续性与跨 LOD 接缝、秋叶树远景和资源回收。浏览器专项通过雨刮／灯光快捷键、公告隔离、换车、三种地形与服务区；十种车型驾驶舱已实拍，水滴轮廓经视觉复查减弱。v0.1.18 页面公告与 CHANGELOG 原文归档并逐字核对。

完整 55 项浏览器回归通过；最后补充挂车压缩读数和底部快捷键提示后，灯光雨刮专项与小窗口驾驶检查再次通过。Pages 项目路径的生产资源、Worker、地形加载检查通过，无脚本或图形错误。

## v0.1.20 — 双向路网、立交与夜行灯光

默认开局使用随机种子，显式 `seed` 查询或手动输入可复现。RoadGenerator 支持有向起点，RoadSpine 按局部前进轴流式生成与检查点重放。起点连接相反方向路线，RoadNetwork 依据玩家位置和高度选择活动道路；岔路出口先生成预览，驶入后延伸，最多保留七条活动道路。渲染总计最多 256 段，各路分别绘制，避免跨路线索引连接。分支祖先仅在本次运行中保留用于返回的生成定义，不写入仓库或存档。

高速在最大坡度至少 2.5%、跨越已有道路净空达到 12 m 时生成环形高架出口，否则使用普通分岔。立交桥面与下层路面分开采样，桥墩避开下层行车范围；路口使用共享的护栏开口描述。返程重建保留入口开口，生成匝道之后清除强制高架标记。桥梁补充盖梁端部、支座底板和高墩检修平台，保留原有箱梁与超高斜拉结构。

十种载具的前后及侧面转向灯使用左右独立材质，挂车同步，0.8 s 闪烁周期；转向超过阈值再回正时取消单侧信号。Q / E / H 控制左、右和双闪；亮度 25–200%、远光 80–800 m。路灯光晕与灯下光斑共用一张纹理，随道路回收，受深度与雾影响；附近动态光仍限制为四盏。擦碰护栏投影至切线并施加轻微摩擦，保留行进速度。UI 改为深蓝青色，加入驾驶路线提示与路口观察入口。

新增回归覆盖有向道路延伸与返回、连续分支选择与缓存容量、立交上下层接触、汽车驶入左右分岔及 20 m 半挂通过约 1.9 km 高架匝道、30 / 60 / 120 Hz 连续护栏擦碰、随机开局与固定种子复现、全车队灯光和公告输入隔离。v0.1.19 原公告完整保留在历史公告。

334 项单元测试、构建和 ESLint 通过。完整浏览器回归首轮 54 / 55 通过，唯一失败为新增灯光纹理后旧预算断言仍写 7；核对十次换种子的纹理稳定为 8，修正断言后 19 项专项检查通过，包含三个新增场景。返程与入口修正后，道路、驾驶、路网和五档路线的 13 项复测通过。Pages 路径检查在原生 GPU 和 CI 尺寸的软件渲染下均通过，软件检查约 1.4 分钟。最新夜景和窄屏实拍未记录脚本或图形错误；进一步修正面板与新增路线提示仪表的窄屏重叠。

## v0.1.21 — 近景模型与渲染开销

树木采用近、中、远三档模型。玩家周围一圈地块使用细化树冠、侧枝与根部；中景保留全部树木，以简化枝叶降低面数；远景保持既有确定性子集。常规植被按 2×2 地块、近景花草按单地块、远景树冠按 8×8 地块批量绘制，各区域维护包围球，可独立参与相机和阴影裁剪。空区域释放实例缓冲，几何与材质共享。实例坐标以所在区域为基准，浮动原点仅移动区域节点，避免重写整片植被。

近景增加起伏阔叶树冠、顶点叶色变化、松树侧枝和根部，石块增加轮廓变化，仙人掌补圆顶。植物几何保留索引。十种载具增加圆润胎肩、胎纹、轮毂与螺栓，车身补格栅、牌照底座和门缝，商用车补踏板与挡泥板；固定网格按材质及阴影属性合并，透明玻璃和运动节点保留独立变换。实体桥墩改为倒角截面，圆柱桥墩细分增加，高墩检修平台增加双层栏杆与立柱。地块统计改为按 HUD 和调试面板刷新频率采样。

性能对比使用 Apple M5 Pro / ANGLE Metal、Chromium、1920×1080、像素倍率 1、种子 `CLOUD-ROAD-001`、4 km 视距、双向四车道、1 档道路、6% 最大坡度、20 m 半挂、跟车视角、晴天 17:12；保持阴影与云开启。生成队列归零后原地取样 10 秒，修改前后相机位置与植被计数一致。三角形与绘制计数包含阴影和后处理通道，并非屏幕实际可见面数。

| 场景 | v0.1.20 三角形／绘制调用 | v0.1.21 三角形／绘制调用 | 植被实例 |
| --- | ---: | ---: | ---: |
| 森林山谷 | 9,072,484 / 940 | 6,290,128 / 809 | 77,492 |
| 高山雪岭 | 8,831,828 / 928 | 6,193,032 / 788 | 80,750 |

森林三角形提交量减少约 30.7%，绘制调用减少约 13.9%；高山分别减少约 29.9% 和 15.1%。两组修改前后均约 60 FPS，p95 帧间隔约 16.7–16.8 ms，取样期间无超过 50 ms 的主线程任务。该设备达到刷新率上限，不能据此宣称帧率提升；其他硬件及移动加载场景需单独测量。

复测命令：`npm run benchmark -- 'http://127.0.0.1:4320/?seed=CLOUD-ROAD-001' --drive --far --highway --terrain=forest --vehicle=semi20 --stationary`。将 `forest` 改为 `alpine` 复测高山；移除 `--stationary` 可测持续驾驶。未指定网址时基准工具使用固定种子。

339 项单元测试、构建与 ESLint 通过，覆盖区域裁剪、包围球更新、长途缓冲释放、LOD 密度与根部位置、刚性部件合并后的边界和法线、独立运动节点及全车队模型预算。完整 58 项浏览器回归通过，更新公告后另行复测当前／历史公告；Pages 项目路径在原生 GPU 与 CI 尺寸 SwiftShader 下均通过。十种载具及森林、秋林、沙漠和高桥完成实拍检查，无脚本或图形错误；新基准命令复现森林计数。v0.1.20 与更早公告已逐字核对归档。

## v0.1.22 — 性能设置、跨谷交通与音频

新增三档画质预设和独立的渲染比例、帧率上限、投影精度与抗锯齿选项。均衡默认 100% 渲染、2048 投影、2× 抗锯齿和 2 km 视距；节能为 65%、关闭投影／抗锯齿／云层及 1.5 km 视距。精细使用 4× 抗锯齿和 3 km 视距，帧率上限不随预设更改。后台停止 RAF 工作，返回重置计时；图形恢复和换世界保留本次配置。界面文字不降采样。

用先失败的回归测试确认道路走廊变化时地块未刷新、旧异步结果被接收的问题，再按地块局部道路／设施快照校验活动、待上传和预加载结果。等价快照继续复用，远处变化不会使整个窗口重建；签名缓存与预加载保持有界，不把签名随地形任务复制到 Worker。

深谷沿道路里程选择可复现的下层道路／铁路景观，要求至少 45 m 地形落差、可接入山体的两端和与现有路网至少 18 m 的中心线高差，保持水平线路。桥面、双柱、四角采样基础、钢护栏、轨枕及钢轨使用实例批次；隧道复用衬砌和地貌覆盖材质。新增路线时重新核对全部附近道路层级，主桥墩与斜拉塔避开下层走廊。最多保留三处，缓存随道路窗口回收，浮动原点只移动节点。景观不是可选驾驶路线，没有列车、交通调度或铁路物理。

新增 Web Audio 合成引擎、风雨、脚步、雨刮与转向灯，以及四声部环境和弦。由显式点击解锁，音效／音乐音量独立，不保存玩家设置。暂停、公告、失焦、后台与图形故障时暂停上下文，恢复复用同一个上下文，销毁释放音源。音频初始化失败只禁用声音，不阻止驾驶；失焦状态保持到焦点恢复。静止时不重复调度相同音频参数。

性能抽样：Apple M5 Pro / ANGLE Metal、Chromium、固定种子 `CLOUD-ROAD-001`、森林、4 km 视距、高速、20 m 半挂、晴天 17:12，生成完成后静止 10 秒，两个配置的相机坐标均为 `137.2, 931.9, 131.0`。这里比较本版本的画质选择，不表示相同画质的加速比例。

| 配置 | 3D 分辨率 | 实际 FPS | 每帧三角形／绘制调用 |
| --- | --- | ---: | ---: |
| 均衡，视距手动 4 km | 1920×1080 | 60 | 6,681,918 / 847 |
| 节能，视距手动 4 km，限帧 30 | 1248×702 | 30 | 3,358,710 / 547 |

两次屏幕刷新回调均约 60 Hz，采样均未发现超过 50 ms 的主线程任务；关闭投影减少额外绘制通道，降低分辨率及限制更新次数进一步控制负载。基准脚本区分实际渲染 FPS 与 RAF 频率，支持 `--quality`、`--scale`、`--fps`。

349 项单元测试、构建与 ESLint 通过。完整 61 项浏览器回归通过，随后补测音频初始化失败、独立通道非零波形、静音／公告／焦点恢复、画质与图形恢复、跨谷观察及新版公告。Pages 项目路径在原生 GPU 和 CI 尺寸 SwiftShader 下均通过。实拍检查森林铁路与大桥的空间关系，核对 v0.1.21 原公告完整归档。

## v0.1.23 — 四季、雨雪与增量景观规划

新增独立于道路生成选项的 `SeasonState`，以地貌基温、季节温差和海拔决定气温、季节积雪及抓地系数。温暖干旱地貌保留低海拔无雪条件。世界内材质复用同一组季节 uniforms，换季不重新请求地形、不改变种子、植被实例矩阵或车辆位置；后续地块、岔路、桥隧、服务区与跨谷景观沿用该状态。地表图案按 4096 m 周期处理浮动原点，朝上法线决定积雪覆盖，保留既有地形和混凝土细节着色器。

落叶树增加春季花簇、冬季裸枝以及对应近／中／远模型；秋季改为金黄至红褐叶色，花草换成枯穗，冬季隐藏野花。原秋色丘陵改称阔叶丘陵，移除固定秋色地表偏色。几何变体共享并在退出世界时释放，只有几何变化的批次重算包围球。路肩有近景落叶斑，冬季保留轮迹，服务区停车标线保留部分对比度。

路面生成携带隧道遮蔽属性，所有驾驶路线接入；轮胎按各自接触高度与匹配路线的隧道判断积雪抓地。天气在低温下混合雨与雪，雪粒子沿用场景深度遮挡、世界锚点、暂停和隧道遮蔽。液态雨量驱动湿路、雨声和自动雨刮，手动雨刮不受限；秋冬微调光照色温，保持用户选择的时钟。侧栏和驾驶仪表增加季节、气温与路况提示，并增加手机面板与仪表间留白。

跨谷景观从整段渲染窗口缓存改为固定候选位置缓存，成功与不适合生成的候选均可复用；坐标或路线发生变化时重新计算。每次路网更新仍用完整走廊复核净空，离开窗口释放缓存。回归验证窗口前移不重复采样旧位置、新分支仍能排除冲突景观，以及候选失效后的重新规划。

验证：355 项单元测试、TypeScript 构建与 ESLint 通过。65 项浏览器全量回归中发现新增仪表行导致手机面板重叠，修复后相关驾驶及四季用例复测通过，其余 64 项通过。四季原地切换不增加已生成地块数、不重置车辆；雨雪暂停、隧道遮蔽、远处服务区、浮动原点、沙漠切换与新季节保持均已验证。逐张检查四季阔叶林、雪路与冬季服务区画面，核对 v0.1.22 的页面公告和仓库公告原文完整归档。

性能抽样：Apple M5 Pro / ANGLE Metal，1920×1080，固定种子 `CLOUD-ROAD-001`、森林、高速、20 m 半挂、4 km 视距、均衡配置、晴天 17:12。车辆位置均为 `137.19, 926.83, 104.99`，稳定后各采样 10 秒；夏季 60 FPS、6,681,918 三角形／847 次绘制，冬季 60 FPS、5,955,102 三角形／843 次绘制，均为 1089 个活动地块、待加载队列 0，无超过 50 ms 的主线程任务。冬季裸枝及休眠野花减少提交量，不代表所有设备或行驶场景均可保持该帧率。基准脚本新增 `--season`。

Pages 项目路径在原生 GPU 与 CI 尺寸的 SwiftShader 下均通过，覆盖资源路径、Worker、四季控件与冬季雪粒子渲染。

季节由玩家手动选择，不保存运行状态；积雪为材质覆盖，暂无自动年历、动态雪层厚度、车辙压雪模拟或真实热传导。

## v0.1.24 — 桥隧选线与高速转向

`StructurePlanner` 沿前方地形识别可跨越的深谷及有出口的穿山段，将直线方向、缓坡、结构起止里程写入道路控制点，按原来的 96 m 路段逐段生成。桥隧计划优先于发卡弯与周期爬升，避让服务区预留区；坡度接线沿用山路／高速的变化限制，返回已释放区域时可从序列化检查点重放。

斜拉桥只接收曲率为零、方向一致、无横坡且纵坡绝对值不超过 1% 的桥面。弯路和陡坡不放桥塔与拉索，深谷普通桥墩间距回落到不超过 192 m；符合条件的斜拉桥继续使用 384 m 塔间距并避让下层交通。道路和路基边缘的 5 m 架桥判定保持不变。

取消旧的 800 m 隧道分段与 120 m 人工露天间隔。单座隧道最长 5000 m，已规划洞口里程随道路检查点保存；检测与渲染窗口可保留隧道中段，开口标记仅表示窗口被裁剪，不绘制假洞口或端部山体封板。覆盖层、路面积雪与天气遮蔽在窗口边缘保持连续，衬砌纹理使用绝对里程。纵向模型行间距改为约 8 m，减少重复构件，长隧道设备与风机容量通过满窗口测试。

世界选项增加山路岔路与高速立交独立开关，应用后重建，换种子时保留本次选择；关闭对应路口仍保留 root / back 两条连续路线。高速立交无法满足条件时，仅在普通岔路开关打开时回退。界面支持键盘和窄屏，已规划桥隧附近不布置路口。

转向在 12–28 m/s 间平滑过渡到按轴距、速度平方及车型稳定性限制的前轮角度；输入仍保持比例，高速不再很早进入偏航限幅。转向变化率随有效锁角收小，保留低速正反转向与固定步长模拟。高山地形加入较大尺度交叠山脊、峰肩与支谷凹蚀，同时保留垭口和山谷引导。

全量浏览器检查发现更换世界后旧景观按钮仍短暂可点、定位请求被忽略的问题。现在同步禁用依赖道路就绪的按钮，并在服务区／垭口请求被接收时立即显示定位进度。旧的陡坡斜拉桥断言改为检查普通支撑，车队雨效用例改用明确的露天草甸路线，避免把新隧道的正常遮雨误报为故障。

验证：378 项单元测试、TypeScript 构建与 ESLint 通过。66 项全量浏览器检查中 63 项通过，上述三处修正后，包含新增按钮同步用例在内的 13 项重点回归全部通过。覆盖 100 km 路线平顺性与回放、4.9 / 5 km 隧道上限、加载窗口中段的几何净空和积雪遮蔽、开关组合、车队、服务区、反向道路及图形恢复。页面与仓库的 v0.1.23 公告均已逐字核对归档。

实景检查：固定森林种子 `CLOUD-ROAD-001` 的约 442 m 深谷桥曲率为 0、纵坡为 1%；实际驶入 x≈2204、z≈−16830 的约 3007 m 隧道，核对洞口、衬砌、设备、冬季路面及遮蔽，浏览器无错误。复查高山垭口和植被。Apple M5 Pro / Metal、1920×1080、4.10 km 视距、冬季森林高速、20 m 半挂静止驾驶视角：约 60 FPS，显示帧间隔 P95 16.7 ms，10 秒内无超过 50 ms 的长任务；约 583 万三角形、834 次绘制。该结果仅代表本机固定场景。

Pages 项目路径在原生 GPU 及 CI 尺寸的 SwiftShader 下均通过，覆盖构建资源、Worker、季节及雨雪渲染。

## v0.1.25 — 每二十公里单出口与环形立交

`JunctionSchedule` 统一 20,000 m 里程周期，每处仅创建一条确定性的右侧支路。起点前后分别计程，支路从入口开始独立计程；移除原来约 7.2 km 的随机双出口。道路提前 1.8 km 开始平滑收坡，出口后约 1.2 km 恢复普通生成；桥隧预留不跨越出口区，服务区位置避让立交并保持约 10–20 km 的间距。

匝道先平缓分流，再沿环形高架转向并跨越主线；交叉处保留至少 12 m 路面高差。山路可使用立交，坡度低于 2.5% 或净空不足时生成单出口平面连接；关闭山路立交升级也保留平面出口。匝道沿用玩家选定的宽度与车行道配置，尚未单独收窄为单车道出口。

新增实例化标志架、分流箭头、引导虚线、路肩斜纹与护栏端头，加入 500 / 200 m 预告牌和局部连续照明，沿用季节材质与浮动原点。护栏与路侧立柱避让连接开口，出口预留区其他位置保持保护。修正长隧道被加载窗口裁剪时的虚假洞口标牌。

「下一匝道」使用有界增量搜索和进度提示，可从起点定位 20 km 处；出口关闭时禁用。浏览器实景复查森林高速环形立交、入口标志架、夜间灯头和照明，页面无运行错误。v0.1.24 页面与仓库原公告完整归档。

自查发现 100 / 300 m 高速周期爬升经过出口预留区时，沿用自然高速的较慢收坡速度会越过目标高度，导致后续循环停滞。周期模式现沿用自身的坡度变化限制收坡，出口之后仍能恢复上下坡；自然高速继续保持原来的缓慢坡度过渡。

验证：391 项单元测试、TypeScript 构建与 ESLint 通过；覆盖 20 / 40 / 60 km 单出口、返回重建、八次连续选择及缓存上限、高山 / 森林 / 喀斯特预留区、5 档与 40% 坡度设置、50 / 100 / 300 / 900 / 2000 m 爬升循环、服务区间距和道路平顺性。敞篷车与 20 m 半挂完整驶出匝道后继续加载。

68 项浏览器全量检查中 66 项通过；其余两项为旧公告标题断言，以及暂停测试在 250 ms 遥测刷新前读取旧坐标。更新标题和等待下一次遥测后，公告、步行、爬升选项、远处单出口定位和路口开关共 6 项定向回归全部通过。

## v0.1.26 — 贴地爬升、固定墩距与连续拱桥

道路纵坡根据每段弯道实际经过的四个地形采样点拟合，并使用阻尼与坡度变化上限保持过渡。取消周期爬升直接覆盖纵坡的逻辑，爬升范围变为选线偏好；平地不再被抬成高架，实际地形下降时允许提前下山。山路折返不再复用过时纵坡，能在最大坡度内爬过的缓山坡不提前安排隧道，服务区接线按距离逐渐收缓。

桥墩统一锚定绝对里程的 48 m 网格，高墩改为随高度连续加粗的实心渐收墩。保留入土基础、支座、盖梁、检修平台与红色钢护栏；下层交通交叉处局部留空，其余墩位不移动。超过 150 m 的平缓跨段加入混凝土拱肋、拱上立柱和横向连接，替代自动斜拉结构。拱形判断以独立跨段为单位，避免相邻弯道进出加载窗口时整段结构消失；拱上支撑使用实际桥面横坡与纵坡，查询改为二分定位。急弯与陡坡继续使用梁桥。

近景草叶改为弯曲叶片，顶点色区分叶根、叶尖和中央叶脉；野花加入花瓣、花萼与茎叶，灌木补充枝干和小叶。双面叶片分别计算法线，避免正反面抵消。花簇按局部噪声形成斑块，保持现有近景范围、季节替换和实例回收，不扩大远景密度。

先添加平地爬升、真实起伏、固定墩距与拱桥净空的失败测试，再实施修正。既有跨谷景观用例改用新选线下仍包含真实深谷的固定种子；高速宽度用例显式设置其要检查的 4% 坡度。v0.1.25 页面与仓库公告逐字核对归档。

验证：393 项单元测试、TypeScript 构建与 ESLint 通过。68 项全量浏览器检查中 66 项通过，另外两项为固定种子下景观类型及桥高改变；选取实际包含铁路和高桥的固定种子后，爬升、多地形、单出口、声音、桥梁及步行共 12 项重点回归全部通过。Pages 项目子路径下的资源、Worker、季节及雨雪检查通过。

实景复查 `VALLEY-22` 森林中约 507 m 深谷的实心桥墩、连续拱肋与检修平台，核对草甸折返路、草叶与花萼。春夏花簇保留，秋冬切换为枯穗或休眠，页面无运行错误。Apple M5 Pro / Metal、1920×1080、4.10 km 视距、夏季森林高速与 20 m 半挂静止驾驶视角：60 FPS，显示帧间隔 P95 16.8 ms，10 秒内无超过 50 ms 的长任务；约 701 万三角形、800 次绘制。性能结果仅代表本机该固定场景。

## v0.1.27 — 工程车队、驾驶调校与 200% 渲染

新增 12 m 四轴平板车、13.2 m 五轴移动吊车、20 m 重载平板半挂，共十三种载具。半挂默认 750 kW（约 1,020 马力），三种新车分别匹配质量、驱动力、制动、转向、悬挂与车内视点；平板车和吊车的双前转向轴按轴位分配轮角。模型补充木质货台、系固点、工具箱、坡板、回转台、伸缩吊臂、配重、收拢支腿、吊钩、警示灯，以及底盘与驾驶室细节。实景复查修正吊钩穿入驾驶室车顶的问题。吊车仅支持行驶，不提供吊装功能。

动力与外观面板提供发动机输出 50–150%、行车制动力 50–150%、低速转向幅度 60–140%、六种车漆及车型原色。换车和换世界保留本次会话的设置；恢复按钮重置三项驾驶倍率。调校不突破轮胎抓地力和高速转向保护，手刹与驻车保留原强度。

渲染比例新增 125 / 150 / 175 / 200%，帧率上限增加 90 / 120 / 144 / 165 / 240 FPS，明确提供不限帧率。输出同时受纹理、渲染缓冲区与视口尺寸约束，缩减时显示提示。浏览器测试复现并修复上下文丢失期间缩放窗口的空值错误。近景植被提供轻量、标准、丰富三档，调整缓存实例的细节范围；预设联动，远景和季节继续保留。地表加入按屏幕像素宽度平滑的细岩层与脉理，岩石周围补充碎石。

先加入失败测试，再实现车队、调校、高刷新率调度及植被细节切换。409 项单元测试、构建与 ESLint 通过；32 项重点浏览器检查覆盖全车队驾驶、新车三种视角、200% 分辨率、设置与上下文恢复、季节、声音、隧道、桥梁、服务区、流式预加载、短窗口和公告。Pages 子路径的资源、Worker 与季节渲染检查通过。v0.1.26 页面及仓库公告逐字归档；不保存玩家运行时状态。

本机性能复查：Apple M5 Pro / Metal，`VALLEY-22` 夏季森林高速，4.10 km 视距、丰富植被、4× 抗锯齿、200% 渲染、不限帧率及重载半挂静止跟车视角。实际输出 3840×2160，10 秒采样约 60 FPS，显示帧间隔 P95 16.7 ms，无超过 50 ms 的长任务；约 797 万三角形、808 次绘制。结果仅代表该硬件与固定场景。

## v0.1.28 — 桥头翼墙、斜拉长跨与体积云

路面和混凝土改为中性灰，降低模板缝、孔隙和水痕反差。箱梁深度按单幅沥青宽度计算，6 / 8 / 10 m 对应 2.12 / 2.36 / 2.60 m；支座、盖梁、墩顶和拱上支撑共同调整。真实桥头增加 14 m 长、向陆地展开的分段翼墙，底部埋入天然地面，避开道路包络与下层线路；窗口裁剪端不生成翼墙。实景检查修复翼墙完全被路基覆盖的问题，压顶按实际填土高度露出，最高不超过邻近路面 30 cm，始终位于道路外侧。

新增独立斜拉跨，要求完整直线桥梁规划、绝对纵坡 ≤1%、两座主墩均 >200 m，并检查投影地形净空及下层道路。中段采用 384 / 576 m 总长的两塔布局，对应 192 / 288 m 主跨；主墩增粗，普通段保持 48 m 墩位和适用的拱结构。高速共用跨双幅道路的门式塔柱，拉索锚点位于行车道外。按规划元数据重建完整斜拉段，修复先加载同规划外侧片段时中央跨被去重遗漏的问题。

高山坡面增加确定性连续沟壑，谷底不增加侵蚀量。云层由固定水平切片改为沿视线最多 24 次体积采样，按场景深度截断，加入两次太阳方向遮光采样和迎光边缘，浓云提前终止积分；保留原有 CPU 云场、漂移、天气与暂停语义。仍是云海视觉近似，没有多次散射或地面云影。

测试先复现桥型阈值、加载重建、翼墙法线、高速斜向净空及重复规划片段的问题，再修复实现。实际森林种子 VALLEY-1 在约 5.0–5.6 km 生成斜拉段；复查道路净空、桥头、山口与晴天、晚霞、夜晚的云海。v0.1.27 页面与仓库公告完整归档，不保留运行时状态。

验证：421 项单元测试、构建及 ESLint 通过。23 项重点浏览器用例通过，覆盖云下／云中／云上、暂停画面冻结、显卡恢复、驾驶、隧道、高速、远距离地块与服务区、公告归档和故障恢复；Pages 子路径资源、Worker 与季节渲染检查通过。

本机 Apple M5 Pro / Metal、1920×1080、4.10 km 视距、夏季森林高速、重载半挂、丰富植被、4× 抗锯齿、200% 渲染及不限帧率：实际输出 3840×2160，10 秒内约 60 FPS，显示帧间隔 P95 16.7 ms，无超过 50 ms 的长任务，约 793 万三角形、814 次绘制。结果仅代表本机固定场景。

## v0.1.29 — 雨刮清水、F 上下车与轻量画质

挡风玻璃使用 64×32 单通道湿度纹理，按雨量积水、速度及停雨时间干燥；两支刮片预计算角度覆盖，清除整帧扫过的区间，跨过反向点也不遗漏。水珠、单侧高光与流痕由玻璃平面着色，不再逐帧更新水珠实例；暂停不改纹理，摩托车无玻璃水膜，隧道使用已有遮雨状态。

F 下车检查车旁地面、桥面、护栏和头部净空，车辆立即驻车并保留姿态、挂车和行程；近旁 F 上车恢复原车，距离较远不能遥控上车。停车模型随浮动原点同步，远处不绘制，换车、换世界和景观定位清除旧车状态。空间不足的提示保持三秒，公告和暂停屏蔽上下车操作。

步行接触改用直接地面高度查询，省去法线和群系计算；同帧同位置复用静止脚底查询，新帧使缓存失效，保留流式地形更新。零速度轴不查询侧向碰撞，步行 HUD 降为 10 Hz 且仅写入变化文本，原有 120 Hz 物理、跑跳和护栏规则保持。

云顶增加细起伏，阴雨衰减直射亮边，浓雾浸没时跳过远云采样；新增 12 / 20 / 28 步云雾细节。将固定版本 Three.js 的 FXAA 函数合入已有合成，提供 FXAA 单独使用或与 2× / 4× MSAA 组合，更新像素尺寸并遵守显卡采样上限；没有额外全屏绘制和渲染目标。

验证：426 项单元测试、构建、ESLint、34 项重点浏览器用例及 Pages 子路径检查通过。覆盖雨水积累、刮片反向、重新积水、暂停、摩托车、桥面 F 上下车、保留停车位置及里程、远离车辆与换世界清理，以及步行、驾驶、隧道、四季、云海、窗口缩放、显卡恢复与公告归档。

本机 Apple M5 Pro / Metal，1440×900、草甸、路旁疾跑 10 秒，同场景步行更新平均耗时从 0.3187 ms 降为 0.0768 ms，P95 从 0.4 ms 降为 0.2 ms；两次均约 60 FPS，仅比较步行 CPU 更新，不代表整帧加速。1920×1080 视口、200% 渲染、4.10 km 视距、精细云雾、4× + FXAA、夏季森林高速及重载半挂固定场景，实际输出 3840×2160，10 秒约 60 FPS，显示帧间隔 P95 16.7 ms、无超过 50 ms 长任务，约 793 万三角形、814 次绘制；结果仅代表本机场景。

## v0.1.30 — 交互与音频审查

先运行全部 72 项既有浏览器基线检查，再针对新增功能补充失败用例。确认长客车在 ±20% 坡道无法下车、腾空车辆可瞬移到地面、上车距离未检查护栏、车旁按钮重新放车、停车附件冻结、恢复音频沿用旧音量及步行 HUD 遮挡问题。

下车地面改为车辆俯仰变换后的驾驶室位置，限制离地高度；上下车共享两侧入口的高度、头部净空和护栏检查。车旁按钮与 F 上车共用恢复路径，远处明确显示重新放置；退出时同步最终里程，进入时立即刷新仪表。收起面板恢复画布焦点，补充无障碍按键提示，较窄窗口限制步行面板高度。

停车期间保留地面查询对象，可见范围内继续更新灯光、转向灯、雨刮及水膜，冻结时使用零时间步长；沿用 250 m 车辆裁剪和换世界清理。音量同步移到 AudioContext 恢复之前，静音声道立即归零；暂停清除瞬态音效，恢复主音量短暂淡入，保留单一上下文与合成音乐。

原生 Web Audio 检查确认静音声道输出为零，并验证恢复前已经安排新音量；AudioParam 的瞬时读取值在暂停状态下可能仍为上一渲染块的值，因此同时检查调度顺序和实际波形。上下车测试比较水平停车位置和行程，允许重新接入悬挂后厘米级垂向回落。新增十三种载具的 F 上下车检查，以及 390 / 480 / 800 像素窗口的 HUD 避让检查。

修复后 56 个文件的 431 项单元测试全部通过（`npm test -- --maxWorkers=2`）；默认并发下三项长距离生成测试曾超出 5 秒时限，降低并发复验通过，未放宽时限。37 项相关浏览器用例及 Pages 项目路径烟雾测试通过，涵盖驾驶、步行、十三种载具、雨水与抗锯齿、音频、焦点、公告和故障恢复；修复前另已通过全部 72 项浏览器基线。构建、lint、补丁格式检查通过，目视确认小窗口步行面板、雨天驾驶舱、停车车辆和新公告。

验证范围为本机桌面 Chromium / Metal 与自动化 Pages 环境；音频检查覆盖混音、静音、暂停、恢复及输出幅度，没有替代不同耳机、扬声器的主观听感检查。沿用游戏物理、简化碰撞和合成音源。

## v0.1.31 — 分类设置、传动与座舱设备

全部设置迁入原生模态弹窗，按驾驶、设备、世界、天气、画质、声音和探索分类。打开时冻结旅程和设备，关闭恢复画布焦点，保持独立暂停状态；设置中的音量与试听仍工作。分类导航按内容区坐标定位并随滚动更新高亮。清键操作移至同步关闭及 beforetoggle，避免延后的弹窗事件清掉刚按下的驾驶键。

新增六挡乘用车／摩托车和八挡重型车的自动、手动传动，转速联动音色，换挡短暂降低驱动力。降挡检查目标转速，升挡允许逐级恢复超转工况；全量回归复现并修复高速车轮被误套降挡保护、导致重型半挂匝道行程不足的问题。停车与回正恢复一挡及怠速，保留本次传动模式。

玻璃水使用 3 L 水箱，喷洗向现有水膜注水并联动补刮，停车可补液。车窗与敞篷分离出静态合批，以简化动画开合；客车与轿车保留后部固定玻璃，摩托车禁用不支持的设备。车顶超过约 5 km/h 暂停，电机音只在实际移动时播放。增加阅读灯、三档通风、按住鸣笛及设备状态；开窗、敞篷改变驾驶舱环境音隔绝程度。

九组音量分别控制总混音、音效、音乐、引擎、胎噪、自然、天气、设备与提示，增加换挡瞬态、湿路胎噪、鸟鸣及夜虫。三种音乐氛围可调节奏，设置中提供引擎／换挡／喇叭试听。保持单一 AudioContext，输出加动态压缩，静音、暂停、公告及后台继续停止音频处理；总音量为零时也挂起。

验证：58 个文件的 441 项单元测试（`--maxWorkers=2`）、完整 82 项浏览器端到端测试和 Pages 子路径检查通过，构建、ESLint 与补丁格式检查通过。覆盖十三种载具、换挡保护、喷水补刮、设备适配、音频混音及暂停恢复、短窗口分类导航、流式世界和故障恢复；本机桌面 Chromium / Metal 目视检查弹窗、封闭车顶及喷洗驾驶舱。

v0.1.30 页面与仓库公告逐字归档，不保存玩家运行时状态。车窗及车顶为简化模型动画，传动没有离合器或熄火，通风没有温控；音源为程序合成，自动化音频验证不能代替不同设备的主观听感检查。

## 已知限制

- 桥型包含程序化梁桥、实心高墩桥、连续拱桥与满足直线、缓坡和深谷条件的斜拉桥，尚无悬索桥；拱结构用于超过 150 m、平直或轻微弯曲且纵坡不超过 1% 的跨段，急弯与陡坡使用梁桥。桥型与截面为视觉近似，没有工程受力模拟。5 m 判定使用道路及路基边缘采样点，桥头在相邻采样间衔接。爬升目标受地形约束，不保证每次达到；全直道、0% 坡度等极端设置仍可能需要长高架或深切坡。道路与设施仍需局部净空切坡，未实现完整地质侵蚀。
- 步行为第一人称简化物理，只处理地面、道路、桥底、隧道和护栏；没有人物全身、建筑、树木、桥墩实体碰撞、摔伤或体力系统。悬崖处可下落，R 返回附近道路。
- 驾驶是多轮弹簧接触与平面转向近似，挂车采用运动学铰接与地面跟随，摩托车自动平衡；不是完整刚体或工程级轮胎模型。没有翻滚、树木、建筑或景观车碰撞，急弯需减速，困住可按 R 回正；20 m 半挂可能无法通过最窄的连续发卡弯，重车不能保证爬上任意湿滑陡坡。SFX 与 BGM 使用轻量合成音源，未使用实车录音；服务区建筑暂无休息或补给交互。植被是原创低多边形模型，尚无风动、积雪几何或 LOD 渐隐，远近切换仍可能看出树冠和密度变化。地貌由用户为整个世界选择，尚未由 Scenic Director 安排沿途切换的体验顺序；温湿度是程序化气候参数。
- 普通折返弯为受约束的约 130–150° 弯道，每条分支沿自己的生成轴推进；立交匝道可绕转跨越母路线。路网是分支结构，没有交通车辆、完整城市路网、自动驾驶或导航寻路。5 档服务区、桥隧和出口附近仍保留缓路，花草与树木没有风动或渐隐。
- F3 道路里程对应水平距离最近的路段，离开路线时不等于实际飞行距离；邻近不同路段的分界处可能切换定位。「道路视角」与「发卡弯视角」都是观察工具，不是自动驾驶。
- 向前或向后重访已释放区域会优先复用有界内存检查点；检查点已被回收或尚未访问的区域仍需从更早位置或起点重放，距离越远等待越长。重放期间道路视角按钮可能暂不可用，换世界清空检查点。
- 服务区在崎岖山地以架空平台避免高填方，但靠山侧仍可能需要较深切坡；快捷视角优先保持相机高于地形，极陡地形中可能从较高处俯瞰。下一服务区搜索复用已有检查点，未访问区域的远距离定位仍需增量生成。
- 云海采用最多 28 次视线体积采样与高度雾，没有多次散射或地面云影；低角度及局部山体交界仍可能看到采样或软边。云场以 8192 m 周期复用，云底固定。极高空或穿入山体的自由相机视角不代表后续驾驶体验。FXAA 处理云雾合成前的场景纹理，不是时域抗锯齿，后绘制的雨雪粒子不经过此滤波；细小植被仍可能闪烁。玻璃水膜没有真实折射流体。
- 天空采用美术化渐变和太阳光晕，尚非物理大气散射；天气和时间手动选择，没有自动循环。阴影为局部单张贴图，没有级联阴影，远景低精度山体不投影，近景边缘仍有分辨率限制。雨天路面调整粗糙度，尚无积水或屏幕空间反射。
- 隧道采用附加衬砌及覆盖网格，不是可任意挖掘的体素地形；没有符合覆盖条件的路段不会生成。单座最长 5 km，强制全直道、极端坡度或超过探测范围的山体仍可能形成深切坡，未实现全局工程选线求解。局部点光源只保留最近四盏，远处灯头仍发光但不投射动态照明，灯具没有独立阴影。
- 超高速飞行或瞬移可以暂时超过生成队列吞吐，页面显示加载进度；旧 LOD 会保留到替换数据就绪。
- 每帧处理预算只能限制启动下一次几何更新，不能中断正在执行的单次更新。帧率目标仍需在不同硬件持续验证。
- 当前种子和地形算法仍在开发，后续算法调整可能改变同一种子生成的地形；不保留未发布版本的兼容层。
- Three.js 使初始 JS 构建超过 Vite 默认 500 kB 提示阈值（gzip 约 250 kB），构建成功。后续按实际加载需求优化拆包。
- 不包含运行时存档、移动端、控制器或 WebGL1 支持。

## 下一阶段

Phase 10 — Scenic Director：按道路里程安排山谷、爬升、穿云、云海、桥梁与雪山的体验节奏，保持种子确定性与流式生成。
